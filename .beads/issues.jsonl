{"_type":"issue","id":"so-x16","title":"Requirements exploration: review/extend requirements.md, resolve C1-C4","description":"Resolve the requirements layer before any design work (R14). Two parts: (1) The user accepts, rejects or amends candidate requirements C1-C4 in requirements.md; (2) sweep for gaps — requirements not yet written down at all. Accepted candidates get renumbered into the R-series; rejected ones are deleted, not archived. Blocks all design issues.","status":"closed","priority":0,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-27T08:54:50Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T14:46:06Z","started_at":"2026-07-29T10:16:54Z","closed_at":"2026-07-29T11:15:23Z","close_reason":"Requirements captured: two candidate rounds resolved, R15-R25 written into requirements.md, and every open decision they leave has its own issue (so-dai, so-joa, so-eqk, so-0zl, so-b09, so-f4m, so-62q, so-219, so-q30).","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-wy3","title":"[BUG] A [petsc] setting silently overrides the [solver] knob the optimiser moved","description":"BOUT++ applies the [solver] settings with explicit calls and then reads the [petsc] section last: snes.cxx calls SNESSetSolverType, SNESSetTolerances, KSPSetType, PCSetType and the rest, and only at line 770 calls lib.setOptionsFromInputFile(snes), which is SNESSetFromOptions. SNESSetFromOptions descends into the KSP and the PC, so an option in [petsc] overrides the matching [solver] one.\n\nThe baseline recipe SNES-MUMPS-3 pins three settings in [petsc] that are also knobs in the search space on the solver side: ksp_type = fgmres, pc_type = lu, snes_linesearch_type = basic.\n\n_set_option in perftest/runner.py edits only the section it is given. So an override of solver:pc_type = ilu leaves [petsc] pc_type = lu in the file. PETSc would run lu; the index row would say ilu. The run is mislabelled, and a mislabelled row is worse than a missing one because it is used.\n\nThe same holds for solver:ksp_type against [petsc] ksp_type, and for solver:line_search_type against [petsc] snes_linesearch_type. The line-search case is half-noticed already: the search space entry says \"Set only one of the two spellings, or they will disagree\", but nothing enforces it and petsc:snes_linesearch_type is not itself a knob, so a proposer cannot set the spelling that wins.\n\nThe precedence above is read from the BOUT++ source and from what SNESSetFromOptions does, not measured. One 0.2 ms window with solver:pc_type = ilu against the unchanged recipe would settle it, and the captured -snes_view would say which PC was built.\n\nOptions for the fix, for the user to choose: refuse an override whose option also appears in the other managed section; or write the override into both sections; or strip the duplicated settings out of the recipes so each setting has one home.","acceptance_criteria":"A campaign cannot produce a row whose recipe says one thing and whose PETSc objects are another. Either the runner refuses the proposal, or it sets both spellings, or the recipes no longer duplicate the setting.","status":"open","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-21T06:05:36Z","created_by":"Mike Kryjak","updated_at":"2026-09-21T06:05:36Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-pep","title":"The runner cannot find the tools that ship beside it","description":"Found by the first real test drive, 2026-09-20. The runs worked; extraction did not.\n\nThe runner calls extract_test.py and can_delete.py by bare name, expecting PATH to supply them. In screen h2 it fails: \"test2_0.0-0.2ms: ERROR [Errno 2] No such file or directory: 'extract_test.py'\". All four runs completed and none was extracted, so four rows sat open as planned with no result.\n\nCause: the three run screens were created on 31 August, and their PATH predates the .bashrc line that adds hermes-solver-opt/cli. They carry sdtools/cli, which is why apply_recipe.py worked and these two did not. Recreating the screens would fix it until the next time they are old.\n\nThe real fix is the one already applied to the window generator: both tools ship in this repository, so resolve them against TOOL_ROOT the way MAKE_WINDOW and DEFAULT_RECIPES are. That stores no path to any machine, because it is relative to the module. apply_recipe.py is different and should stay a PATH lookup: it belongs to sdtools, not here.\n\nNote for whoever does it: the runner carried on to the next trial after the error rather than stopping, which is right, but it means a whole campaign can finish with every row unextracted. Consider whether a failure to run a tool at all, as opposed to a tool that ran and refused, should stop the loop.\n\nWorked around for the test drive by putting the directory on PATH in the runplan line, which is not a fix.","acceptance_criteria":"extract_test.py and can_delete.py are resolved against TOOL_ROOT, not PATH. A campaign runs end to end in a screen whose PATH lacks hermes-solver-opt/cli, and every row ends up extracted.","notes":"Done 2026-09-20 in ec3878b: EXTRACT and CAN_DELETE resolve against TOOL_ROOT, run_tool turns an OSError into Stop, and the end-of-campaign line counts every trial. Four tests added, three of which fail against the previous runner.\n\nNot done, and it is the rest of this issue's acceptance criterion: both tools carry `#!/usr/bin/env python3`, so an old screen still runs them under the pyenv shim, whose python has no pandas. Finding the file is fixed; choosing the interpreter is not. Checked: `env PATH=~/.pyenv/shims:/usr/bin:/bin python3 -c \"import pandas\"` fails with ModuleNotFoundError, and perftest/logparse.py imports pandas at module level, so a real extraction dies there even though --help works.","status":"closed","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T18:18:11Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T20:52:09Z","closed_at":"2026-09-20T20:52:09Z","close_reason":"Fixed in two commits on 2026-09-20. The tools resolve against TOOL_ROOT, and they now start under the interpreter that is running the runner rather than under whatever python the shell offers, which was the other half of the same failure. A tool that cannot start at all raises Stop, so the loop ends with the budget unspent instead of producing a campaign of unextracted rows, and the closing line counts every trial. Six tests, five of which fail against the previous runner. Checked against a PATH holding only the pyenv shims and /usr/bin. The clean drive with no PATH workaround in the runplan line belongs to the test-drive issue, which stays open for it.","dependencies":[{"issue_id":"so-pep","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T19:18:16Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-o25","title":"The runner launches in a way this workstation blocks","description":"Raised by the agent that wrote the runner, and not resolved.\n\ncampaign.toml's [machine] launch calls the launcher directly, following design.md's rule that the screen and run-plan convention stays out of the tool. This workstation's pre-tool hook blocks exactly that call, because the run-case skill owns launching and requires a slot, a screen and a runplan line.\n\nSo the runner as written cannot launch on this machine. Three ways out: the campaign's launch command becomes whatever the run-case skill permits; the runner is driven from inside a screen so its launch is the skill's launch; or the runner writes a runplan line and stops, leaving the launch to a person.\n\nThis blocks the end-to-end test drive of a tiny campaign, so it needs settling before the hackathon uses the runner for anything real.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T17:32:35Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:50:33Z","closed_at":"2026-09-20T17:50:33Z","close_reason":"Ruled by the user: the run-case skill now admits a driver. Step 5 gains two lines saying a driver that launches its own runs takes one runplan line for the driver, with a comment naming where the driver records the runs it starts, so runstat.py can still find them. The runner already obeyed every other launch rule: the sanctioned launcher, the slot flag, the no-prompt flag, inside a screen. Scored 7/7 against the instruction checklist after a first draft failed on the trigger, the line count and a contradiction with step 5. Committed in agent-instructions and installed.","dependencies":[{"issue_id":"so-o25","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T18:32:42Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-avb","title":"The runner's kill outcome never reaches the index","description":"Found by reading the seam between the two pieces written in parallel on 2026-09-20.\n\nIn Runner.run_trial the order is: launch, then extract, then update_row(outcome=KILL_OUTCOME[kill_reason]). The extractor now fills outcome for every run it can classify, and update_row writes blank cells only, with state the single exception. So the runner's write is silently dropped.\n\nWhat it costs: a run the runner deliberately killed has no finish stamp and no error message in its log, which classify_outcome reads as crashed. A deliberate stop is then recorded as a death. Scoring is unaffected, because best_wall counts only completed rows, so neither crashed nor timeout can move the cutoff. The damage is to the record.\n\nTwo ways out, and this needs a ruling rather than a coin toss.\n\n1. The runner stops the run in a way the log records: the BOUT stop file, or the clean-exit signal. classify_outcome then decides from evidence and stays the single decision point, and KILL_OUTCOME is deleted. A cutoff kill still classifies as timeout, because elapsed \u003e= cutoff_s is tested before the cancelled rule. Cost: a genuinely hung run never reads its stop file, so a hard kill has to stay as the fallback, and that case still lands as crashed.\n\n2. update_row overwrites outcome deliberately, as it already does for state, on the grounds that the runner knows something the log cannot: that a person or a budget stopped the run. Cost: two places now decide an outcome.\n\nOption 1 is the better principle and option 2 is the smaller change. Neither is started.","status":"closed","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T17:32:12Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:50:31Z","closed_at":"2026-09-20T17:50:31Z","close_reason":"Fixed by narrowing the blank-cells-only rule rather than changing how the runner stops a run. update_row takes an overwrite list naming the columns one call may replace, and only the kill branch names outcome; a run that died on its own keeps whatever the extractor read from the log. Verified by breaking the fix and watching the tests fail: the kill test asserted timeout and got completed. 138 tests pass. The test that should have caught this originally stubbed extraction out, so the cell was blank and the correction had nothing to overwrite; it now writes the outcome a real extraction would.","dependencies":[{"issue_id":"so-avb","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T18:32:26Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-qu2","title":"Parent dumps must survive until their seeds are cut","description":"The test5 parent's dumps were deleted on 2026-09-20 at 15:25, four minutes after its record was committed to the store at 15:21. The directory mtime proves it: test2 and test4 parents still carry mtime 2026-09-19 20:14 and their ten dumps each, while test5's directory was rewritten today and holds zero BOUT.dmp files. Only the logs and the 100 ms endpoint restarts survive.\n\nNothing malfunctioned. An earlier session applied the project's own retention rule - the store is the surviving record, dumps are expendable once extracted - with disk at 89 percent. The rule is right for an ordinary run.\n\nThe defect is that a parent run is not an ordinary run. Its dumps are also the seed source, and seeds can only be cut from them. Deleting them after extraction destroys the ability to cut any seed except at the endpoint, which is exactly what happened. The retention rule and the seed rule contradict each other for parent runs, and nothing in the tooling or the procedure notices.\n\nFix: a parent run's dumps are protected until every seed on its ladder has been cut and verified, and only then deleted. This needs to be enforced somewhere a script checks, not only written in a document, because the deletion was automated. Candidates: a marker file in the case directory, a column in the index, or a refusal in whatever performs the deletion.\n\nCost of the miss: the test5 parent took 16.6 hours and must be re-run from scratch.","status":"closed","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:30:10Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:51:54Z","closed_at":"2026-09-20T15:51:54Z","close_reason":"cli/can_delete.py refuses deletion for a parent run whose seeds are not all cut, and step 5 of the run procedure now calls it. Committed in 7fd3b91. The parent signal is the index seed column, not the directory name, so it survives a rename. Verified against all three parents and two ordinary windows: test5 refuses, test2 passes once its seed set is stated, test4 refuses when a seed time it lacks is named.","dependencies":[{"issue_id":"so-qu2","depends_on_id":"so-dbq","type":"parent-child","created_at":"2026-09-20T16:30:15Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-or2","title":"Split the project into hermes-solver-opt, and rename the store","description":"Ruled by the user 2026-09-20. The durable layer (instructions, design, requirements, runner, extractor, tracker) goes into a new public repo /home/mike/work/hermes-solver-opt, with hermes-perftest and sdtools pinned as submodules, so one recursive clone is the whole machine. The record and the analysis go into the store, renamed from perftest-results to solver-opt-store, which also takes the live run area and the seeds as ignored directories.\n\nPhases: 1 skeleton and submodules; 2 move documents, skills and .beads out of notebooks; 3 move perftest/ and its CLIs out of sdtools, leaving logparse, case_comparison and the launcher; 4 move and rename the store, add analysis/; 5 move the run area once test5 exits; 6 repoint every instruction file and verify_store's hardcoded root; 7 then the extraction fixes in so-xkv.\n\nPhase 1 done 2026-09-20: repo created public at github.com/mikekryjak/hermes-solver-opt, submodules pinned at hermes-perftest c36d4c8 (window-scheme) and sdtools 0f351e6.","notes":"Phase F done 2026-09-20, and the split is complete. The run area moved to /home/mike/work/solver-opt-data/cases (42 entries incl. the three grids) and the seed library to solver-opt-data/seeds; /home/mike/work/cases/perftests/solver-opt no longer exists. Dumps deleted for the five runs extracted today, freeing about 1 GB; restart files kept, so windows can still be cut from the parents. Runplans 1 and 2 retargeted and their finished lines cleared. verify_store DEFAULT_ROOTS is now the single new root; a full verify before the move was 29 of 29 matching. Remaining gaps live in their own issues: slot/hermes_branch empty (launcher flush, sdtools), failure-step tabulation, ne_sep/te_sep convention, conduction_method.","status":"closed","priority":1,"issue_type":"task","assignee":"Mike Kryjak","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T11:07:21Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T14:26:47Z","started_at":"2026-09-20T11:07:38Z","closed_at":"2026-09-20T14:26:47Z","close_reason":"The project is split: hermes-solver-opt holds the durable layer and the tracker, solver-opt-store the record and the analysis, solver-opt-data the cases and seeds outside git. Instructions, paths and memories follow.","dependencies":[{"issue_id":"so-or2","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-09-20T12:08:54Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-xkv","title":"Move the extraction layer into hermes-perftest and fix its gaps (R9/R10/R23)","description":"Decided by the user 2026-09-19: focus on the extraction layer before anything else, because fixing it may require rerunning the tests. The extractor moves out of sdtools into hermes-perftest (the tool repo per design.md; its window-scheme branch holds make_window.py). The user owns and commits hermes-perftest; every code edit needs the user's execute.\n\nOpen before starting:\n- Where in hermes-perftest (e.g. a solver-opt/ or extraction/ directory on window-scheme). Not yet asked.\n- sdtools/perftest/ is about 1,000 lines with tests and imports sdtools/hermes3/logparse.py, which other sdtools tools share. Proposed: move perftest/, keep logparse in sdtools and import it. The user has not ruled. Removing perftest/ from sdtools is a commit to a public repo: ask first.\n- solver-opt CLAUDE.md says capture and extraction tools belong in sdtools; update that line via the edit-instructions skill.\n\nFixes to make once moved (gaps listed in performance-data.md, commit d887127):\n1. outcome: extract.py:823 labels any run whose log holds the SNES-failed marker as snes_failure, but BOUT++ prints it on every recovered failure. Completed runs with recovered failures must be 'completed'; SNES and linear-solver failures are normal. Add tests; none pin outcome today.\n2. slot and hermes_branch empty: the sdtools launcher (cli/sd-run) prints its banner into the tee pipe and then replaces itself with mpirun via execvp without flushing, so the banner never reaches BOUT.log.console. Fix: flush stdout before execvp. The launcher stays in sdtools.\n3. Failure steps not tabulated (snes_steps.tsv holds converged steps only); failure-block field ranges not extracted; no snes_view/ksp_view in recipes; diffs ignore physics settings; seed shows a z placeholder for scratch runs; concurrency stored as a fraction; conduction_method never filled; ne_sep/te_sep empty (no separatrix convention).\nThen review performance-data.md question-first with the user, decide the extra capture the optimiser needs, and decide whether today's runs must be rerun.\n\nState of runs at handoff (2026-09-19, about 21:45):\n- Parents done and recorded: test2_0.0-100.0ms-20260919-201426 (54 s) and test4_0.0-100.0ms-20260919-201426 (41 min). Dumps kept. Seeds in /home/mike/work/cases/perftests/solver-opt/seeds/\u003csha\u003e/\u003cparent case\u003e/\u003cstart\u003ems/.\n- test5_0.0-100.0ms-2026-09-19-neutlag-va running on slot 2 (about 10 min per ms, thousands of recovered failures). Extract when done.\n- Windows on slot 1: test2_0.0-1.0ms (30 s) and test2_20.0-21.0ms (9 s, mostly restart cost) finished; test4_3.0-4.0ms running; test4_20.0-21.0ms queued. Rows open as planned; extract all four when done.\n- Runplan hygiene: runplan2 still holds the finished test2 parent line. Remove it only after the slot 2 queue (test5) exits, since editing a script bash is executing is unsafe. Clear runplan1 lines once its windows finish.\n- Uncommitted for the user in hermes-perftest: make_window.py seed path now keyed by parent case.\n","notes":"2026-09-20, second pass: three more gaps closed. physics_diffs compares a case's whole BOUT.inp against the input of the run it restarted from, naming changed keys outside [solver]/[petsc]; cli/add_views.py adds snes_view/ksp_view per case (undashed, as BOUT takes petsc flags) and they are excluded from the recipe diff so they never read as an unintended change; the 36-z null restart id is no longer recorded as a seed. The concurrency item was wrong: that column is a time-weighted mean count of simultaneous runs, documented and deliberate, and needs nothing. conduction_method is deprioritised by the user 2026-09-20 on the assumption it will not change. The launcher flush is committed in sdtools (0a3ae44) but not pushed. 79 tests pass. That closes the list this issue opened with.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-19T20:44:00Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T14:54:33Z","closed_at":"2026-09-20T14:54:33Z","close_reason":"The extraction layer moved into hermes-solver-opt and its gaps are fixed: outcome, failure tabulation, separatrix pair, physics diffs, solver views, seed placeholder. What remains is tracked elsewhere - conduction_method (deprioritised), the six-state outcome taxonomy, and the sdtools push.","dependencies":[{"issue_id":"so-xkv","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-09-19T21:44:07Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-fhv","title":"Test drive: a tiny campaign end to end","description":"Run the loop from a clean checkout in a fresh Python environment with no path under the user's home in any config. One small test4 campaign: the baseline plus a few variants on rung 0, one promotion. Times rung 0 so the ladder can be retuned, and proves the portability claim.","notes":"Ran 2026-09-20 evening, on slot 2, and it worked. Four trials: two test2 0.0-0.2 ms windows at 26 s and two 20.0-20.2 ms windows at 9 and 8 s, all completed and recorded in a fresh per-campaign index at solver-opt-store/campaigns/test-drive/index.tsv. Windows were cut from existing seeds, recipes applied, rows opened before launch, dumps extracted and validated.\n\nResumability is proved: after the first pass failed to extract, re-running the same command recognised all four finished runs, extracted them and relaunched nothing.\n\nTwo faults found, both filed and neither fixed: the runner cannot find the tools that ship beside it, and the screens' python is the pyenv shim which bus-errors on pandas. Both were worked around by fixing PATH in the runplan line.\n\nOne thing the per-campaign store exposed: every extraction warned \"physics_diffs not computed: no recorded run has the seed's run_id\", because the campaign index does not contain the parent's row. That is for the per-campaign store issue to settle.\n\nOne result: raising the GMRES restart from 30 to 100 changed nothing, because these windows take one linear iteration per Newton step and never approach a restart at 30. The rung-0 windows cannot test that knob.\n\nSTAYING OPEN deliberately: this drive needed two PATH workarounds in the runplan line to get through. A clean drive, with the runner finding its own tools, is what closes this. See the tool-path bug.","status":"open","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:44Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T18:20:19Z","dependencies":[{"issue_id":"so-fhv","depends_on_id":"so-4hv","type":"blocks","created_at":"2026-09-18T13:42:06Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-fhv","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:02Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-fhv","depends_on_id":"so-ccd","type":"blocks","created_at":"2026-09-18T13:42:06Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-fhv","depends_on_id":"so-o25","type":"blocks","created_at":"2026-09-20T18:32:42Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-fhv","depends_on_id":"so-pep","type":"blocks","created_at":"2026-09-20T19:18:17Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":4,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-drc","title":"Search space file: the knob dictionary","description":"One declarative entry per solver knob: type (switch, choice, integer, continuous), range or values, which solver it applies to, what it depends on, and a note on what it trades. Written from what is known now and marked incomplete. This is the artefact the intelligent mapping of the space will be built on, and what the user takes into the DeepMind conversation.","status":"closed","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:43Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:32Z","closed_at":"2026-09-20T15:35:32Z","close_reason":"The knob dictionary is written: search-space.toml, 62 knobs in six groups, committed in 2915563. Every solver: name verified programmatically against the BOUT++ SNES source; PETSc-side defaults taken from the captured view rather than the manual. The view mining is folded in - the GMRES restart mismatch, the unread colouring flag and the scale_vars defect are all recorded as entries.","dependencies":[{"issue_id":"so-drc","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:01Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-nyf","title":"sdtools: remove the six hardcoded paths","description":"Add pyproject.toml so sdtools installs anywhere, and move hardcoded defaults such as verify_store.py's search root into config. The user reviews the diff and pushes; sdtools is their public repo.","notes":"2026-09-20, the user decided NOT to package sdtools, and the title now says what survives. Counted first: the bare import style dominates, 'from hermes3.' alone has 2808 uses against 85 for the sdtools-prefixed form, so renaming the import root is off the table. Packaging as-is would claim five very generic top-level names - case_comparison, cli, hermes3, sd1d, solkit - in any environment it was installed into, and the current per-process path append is strictly safer.\n\nWhat survives is the hardcoded paths: six /home/mike paths remain, confirmed 2026-09-20, including case_comparison/campaign.py:18 and cli/sdrun.py:220.\n\nChecked 2026-09-20 and now stale: this note used to say commit 0a3ae44 was unpushed. It is on origin/main.\n\nsdtools is the user's public repo, so the user reviews the diff and every push is asked for separately.","status":"open","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:43Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T18:05:19Z","dependencies":[{"issue_id":"so-nyf","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:01Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-8pt","title":"Outcome taxonomy in the extractor","description":"Six states, each decidable by script from the log and the dump: completed, invalid, diverged, timeout, crashed, cancelled, plus the plasma time reached. Failed runs stay as rows with blank timings. A crash never counts as a bad score. Cutoff at two to three times the best time for that window is recorded as a timeout.","notes":"2026-09-20: the completed-with-recovered-failures case is fixed in classify_outcome, and fail_reasons now records how the failed solves failed. The rest of the six-state taxonomy (invalid, diverged, timeout, crashed, cancelled) is still to do.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:41Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:32:44Z","closed_at":"2026-09-20T17:32:44Z","close_reason":"Delivered 2026-09-20 and committed. Extractor: six outcomes decided from the log and the dump, divergence tested before the clock, an interrupt never read as a crash, ms_per_24h written only for a completed run; CVODE counts now come from the dump's cumulative counters, last value not summed, verified against both CVODE rows. Campaign config and runner: campaign.toml with an enforced approval record, the section 8 loop, resumable from the index, cutoff set only by completed runs. Reports: an outcome page before any timing, DNF for a blank wall clock, the convergence-test bar shown as each run's own share with the count kept as a label, PDFs written to the data directory. 136 tests pass. Two gaps filed separately: the runner's kill outcome never reaching the index, and the launch call this workstation blocks.","dependencies":[{"issue_id":"so-8pt","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:59Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-8pt","depends_on_id":"so-xkv","type":"blocks","created_at":"2026-09-19T21:44:08Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-jnn","title":"Query script: never read fifty columns","description":"A script that filters, groups and ranks the index and prints a projection of about ten columns. Neither the user nor a model reads the raw table or aggregates it by eye: measured work shows model reading fails at counting and filtering over tables while single-value lookup is near-perfect.","status":"closed","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:40Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:33Z","closed_at":"2026-09-20T15:35:33Z","close_reason":"cli/query.py is written and committed in 2915563. Filters, sorts, groups on medians, validates column names against the header, prints ten columns by default. Verified against the raw file with awk on seven separate counts and medians.","dependencies":[{"issue_id":"so-jnn","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:57Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-nkj","title":"Per-campaign store: append-only index, merge rule, validator","description":"Each campaign keeps its own index.tsv and runs/ bundles. Make the index append-only, set the git merge rule so parallel appends never conflict, add a schema file describing the columns, and a validator that checks for duplicate run identities and column drift.","notes":"2026-09-20, ruled by the user: campaign results stay on this machine and do not go to GitHub. campaigns/ is now in the store repo's .gitignore, and the commit that had put one there was dropped from history and force pushed, so it is not in the remote either.\n\nWhat this settles for this issue: a per-campaign index is local state, not committed record. The main index.tsv stays committed exactly as it is. So the append-only index, the merge rule and the validator have to work without git as the sync mechanism for campaign results, and whatever merges a campaign's rows into the committed index is the point where results become shared.","status":"open","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:39Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T19:37:10Z","dependencies":[{"issue_id":"so-nkj","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:57Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-2qh","title":"Campaign config: goal, build, space, ladder, budget, approval","description":"campaign.toml as designed: goal, fixed build, baseline recipe, search space reference, ladder rungs and noise bounds, budget in slot-hours, disk budget and retention rule, correctness tolerances, approval record. The runner refuses to start without the approval record.","status":"closed","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:38Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:32:44Z","closed_at":"2026-09-20T17:32:44Z","close_reason":"Delivered 2026-09-20 and committed. Extractor: six outcomes decided from the log and the dump, divergence tested before the clock, an interrupt never read as a crash, ms_per_24h written only for a completed run; CVODE counts now come from the dump's cumulative counters, last value not summed, verified against both CVODE rows. Campaign config and runner: campaign.toml with an enforced approval record, the section 8 loop, resumable from the index, cutoff set only by completed runs. Reports: an outcome page before any timing, DNF for a blank wall clock, the convergence-test bar shown as each run's own share with the count kept as a label, PDFs written to the data directory. 136 tests pass. Two gaps filed separately: the runner's kill outcome never reaching the index, and the launch call this workstation blocks.","dependencies":[{"issue_id":"so-2qh","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:56Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-4hv","title":"Runner loop: prepare, launch, watch, extract, prune","description":"The ten steps in design.md section 8. Orchestrates existing tools as subprocesses, never reimplements them. State lives only in the index, so it is resumable. Checks approval, budget in slot-hours and the disk floor before launching; kills on stall or cutoff and classifies the outcome. Generalises perf-bisect's run_ladder.py.","status":"closed","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:38Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:32:45Z","closed_at":"2026-09-20T17:32:45Z","close_reason":"Delivered 2026-09-20 and committed. Extractor: six outcomes decided from the log and the dump, divergence tested before the clock, an interrupt never read as a crash, ms_per_24h written only for a completed run; CVODE counts now come from the dump's cumulative counters, last value not summed, verified against both CVODE rows. Campaign config and runner: campaign.toml with an enforced approval record, the section 8 loop, resumable from the index, cutoff set only by completed runs. Reports: an outcome page before any timing, DNF for a blank wall clock, the convergence-test bar shown as each run's own share with the count kept as a label, PDFs written to the data directory. 136 tests pass. Two gaps filed separately: the runner's kill outcome never reaching the index, and the launch call this workstation blocks.","dependencies":[{"issue_id":"so-4hv","depends_on_id":"so-2qh","type":"blocks","created_at":"2026-09-18T13:42:09Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-4hv","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:55Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-dbq","title":"Parent runs and the seed library","description":"Run test4 and test5 from scratch at the fixed build with the baseline recipe, then cut seeds at the ladder times with make_restart.py into \u003cdata\u003e/seeds/\u003csha\u003e/\u003ctest\u003e/. Needs the user's build and baseline recipe first. Parent dumps are deleted once the seeds are cut.","notes":"2026-09-20 evening, the open decision in the note below is MADE: re-run. The byte-identical test5 parent repeat started on slot 1 at 17:00 on 20 Sept and finishes around 08:00 on 21 Sept. Cutting its seeds at the ladder times is the next action the moment it finishes, and must happen before any deletion, because a parent's dumps are the only seed source.\n\nStill true from the earlier note: test5's original parent dumps do not survive, only the ten logs and the final restart files. Seeds that exist today are test2 at 0.0 and 20.0 ms and test4 at 3.0 and 20.0 ms, under commit ef2ef9dd. The test2 and test4 parent dumps are also still on disk and were meant to be deleted once their seeds were cut.","status":"open","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:37Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T18:05:55Z","dependencies":[{"issue_id":"so-dbq","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:55Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"so-ccd","title":"Window generator: make a test from a parent seed","description":"Generate a window case from the seed library: copy the parent test definition, set nout=50 and the timestep so the run ends at time b, seed the restart files from \u003cdata\u003e/seeds/\u003csha\u003e/\u003ctest\u003e/\u003ca\u003ems, apply the recipe and overrides. Naming on the 0.1 ms grid, e.g. test4_1.0-2.0ms. Lives in the tool, calls sdtools for the per-case steps. See design.md section 4.","notes":"2026-09-20 evening: the open question about the recipe split is ANSWERED, by the campaign runner written the same evening. The runner applies overrides itself, writing recipe-applied.txt into the case and applying it with apply_recipe.py, so the window generator is right not to. The split stands as it is and needs no change.\n\nStill open, and only this: the generator has never run for test5, which waits on the test5 parent repeat finishing around 08:00 on 21 Sept and its seeds being cut.\n\nAlso worth knowing for the runner: make_window.py lives in the hermes-perftest submodule and is not on PATH, while the runner invokes it by bare name. Resolving it repo-relative, the way the recipes directory already is, is a separate small fix.","status":"open","priority":1,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:36Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T18:05:56Z","dependencies":[{"issue_id":"so-ccd","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:54Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-ccd","depends_on_id":"so-dbq","type":"blocks","created_at":"2026-09-18T13:42:07Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-4tc","title":"Hackathon system: the two-layer runner and optimiser","description":"The system designed on 2026-09-18 for the DeepMind hackathon, 2026-09-21 to 2026-09-23. Design is in design.md; the decisions behind it are candidates C8-C19 in requirements.md. Everything under this epic must be ready or deliberately deferred before Monday.","status":"open","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:40:59Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:41:35Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-pp3.2","title":"Jacobian pruning is untried and attacks the 84 percent colouring share","description":"Jacobian pruning is untried and points straight at the 84 percent. It is the\nonly lever left on Jacobian construction after tightening tolerance, matrix-free\nand lag-the-assembled-Jacobian all closed (so-erp).\n\nMechanism. `prune_jacobian` (snes.cxx:124) scans the assembled Jacobian after\nevery rebuild, and if more than `prune_fraction` of entries fall below\n`prune_abstol` it calls `MatFilter`, compacts the nonzero pattern, and RECOMPUTES\nTHE COLOURING. The colour count is exactly the number of residual evaluations per\nJacobian build - about 160 on test5. Cutting it cuts the 84 percent directly and\nproportionally.\n\nWhy there should be anything to cut. BOUT++ builds the sparsity pattern \"via\nstencil assumptions\" (include/bout/petsc_preconditioner.hxx:5, :39) - every\nevolved variable assumed to couple to every evolved variable at every stencil\ncell. That is a conservative superset, not the true sparsity. Spatially there is\nnothing to prune; the prunable part is the all-to-all block in the FIELD\ndimension, where most pairs have no direct coupling (impurity density does not\nenter main ion momentum except through sources). Colours are roughly stencil\nfootprint times number of fields, consistent with 160.\n\nNever tried. `prune_jacobian` defaults false and appears in no recipe, no run\nrecord and no finding.\n\nTwo regimes, and only the first is free.\n\n(a) prune_abstol = 1e-16, the default. Against normalised values of order one\nthat is machine epsilon, so it removes only entries the pattern allocated and the\nphysics never writes. The Jacobian is unchanged as a mathematical object and the\nonly effect is a cheaper colouring. If the pattern is as conservative as the\nheader says, this is a pure speedup at zero convergence cost.\n\n(b) prune_abstol raised. Now real couplings go, and it becomes colour count\nagainst failure count - the same cascade that already dominates, since a failure\nresets the Jacobian lag (snes.cxx:857).\n\nTRAP, and the reason this is not just a setting to flip. The restore path\n(snes.cxx:860) requires `4 * lin_its \u003e 3 * maxl` - Krylov iterations near the cap,\nsignalling a bad preconditioner. In this configuration `lin_its` EQUALS `nl_its`,\none Krylov iteration per Newton iteration, because matrix_free_operator=false\nmakes the assembled matrix the operator and MUMPS factorises it exactly (so-8b8).\nThat condition can never be true here, so an over-pruned Jacobian is never\nrestored. Worse, pruning the operator does not slow the Krylov solve at all - LU\nstays exact for the pruned matrix - it silently returns an inexact Newton step.\nThe damage appears only as line-search failures, which is precisely the failure\nmode already dominating test5 (79 percent LINE_SEARCH, so-erp). Any regime (b)\nwork needs a guard watching Newton convergence and failure rate, not Krylov\nsaturation.\n\nSecond hazard for regime (b): MatFilter compacts the pattern, so a pruned entry\nis invisible until a full restore. A coupling that is negligible while attached\nand essential after detachment would be silently absent. Hermes-3 is full of\nstate-dependent branches - slope limiters, upwind direction, floors, softFloor,\nreactions that vanish at a density floor - so \"zero at this iterate\" and\n\"structurally zero\" are different sets.\n\nFirst move is cheap and decides whether the rest is worth doing: one short test5\nsegment, prune_jacobian=true at the 1e-16 default with diagnose on. The code\nprints \"Pruning Jacobian elements: X / Y\" (snes.cxx:155). That ratio is how much\nof the assumed pattern is structurally empty. Then compare colour count and\nwall-clock against the same segment unpruned.\n\nAlso relevant to the hackathon pitch: regime (b) is a search problem under a\n34-hour objective with an unguarded failure mode, which is the shape worth taking\nto an external collaborator.\n","acceptance_criteria":"Measured on a short test5 segment: the pruned fraction X/Y at prune_abstol=1e-16, the colour count (residual evaluations per Jacobian build) pruned vs unpruned, and wall-clock, Jacobian builds and failure count for both. A statement of whether regime (a) is a free win, and if so whether the BOUT++ default should change.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-08T17:28:22Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:31Z","closed_at":"2026-09-18T12:42:31Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-pp3.2","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-09-08T18:28:21Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-bt1","title":"scale_vars floors zero-valued elements at rtol, amplifying their residual by 1/rtol","description":"THE DEFECT. BOUT-dev src/solver/impls/snes/snes.cxx:627-635 computes the per-element scale factor as\n\n    norm    = max(|x_i|, rtol / scale_i)      // scale_i starts at 1\n    x_i    /= norm\n    scale_i = norm\n\nand the residual is then divided by that scale (snes.cxx:1494, VecPointwiseDivide(f, f, var_scaling_factors)). Any element whose value is exactly zero therefore takes scale = rtol, and its residual is amplified by 1/rtol = 1e5 at the default tolerances. Those elements then dominate the residual norm and the convergence test, and the solver is effectively asked to converge empty cells to an absolute tolerance of about atol*rtol.\n\nPROVEN, not inferred. Same case (test4scratch), same state, only rtol changed:\n\n    scale_vars off, rtol 1e-6    initial residual         26.9651\n    scale_vars on,  rtol 1e-6    initial residual 19,360,394.5437594\n    scale_vars on,  rtol 1e-5    initial residual  1,936,039.4543759\n\nThe two scaled residuals are in the ratio 10.000000 — exactly 1/rtol. That can only happen if the norm is dominated by floored elements.\n\nWHY IT MATTERS. It makes scale_vars unusable from scratch in some configurations, which is not a reasonable thing to ask users to work around. Observed consequences:\n  - VanAlbada builds from scratch with scale_vars FAIL at the first step (SNES reason -9, DIVERGED_DTOL, then -5). MC builds from scratch survive the identical scaling and identical initial residual. Record: perftests/test4dev/test4scratch-2026-07-2{6,7,9}-* , three directories suffixed -fail, plus 07-29-master-mc-mumps2 which completes.\n  - It killed four ST40 high-resolution runs on 2026-08-25 (fluxlim/st40-flim/st40fl9a-d, all void). Diagnosis in separate-neutral-fluxlim/implementation_log.md, 2026-08-25.\n\nTHE STRUCTURAL PART NO INPUT CAN FIX. Momentum fields (NVd, NVd+) are exactly zero at t=0 in every cold start, and correctly so. So a from-scratch run always has floored elements and always reports 'Var scaling: 1e-05 -\u003e ...'. Setting non-zero initial densities and pressures removed the FATAL cases (those were the zero fields carrying explosive ionisation/recycling sources, ddt(Pd) reaching 1e17) but did not remove the floor itself.\n\nPROPOSED FIX. Floor the scale per FIELD rather than per element at rtol — e.g. max(|x_i|, floor_frac * max|x| over that field), so an empty cell inherits the scale of the field it belongs to instead of a pathological one. Roughly five lines in snes.cxx. Alternatives worth weighing: expose the floor as its own option instead of reusing rtol (rtol is a convergence tolerance and has no business setting a variable scale); or exclude floored elements from the convergence norm.\n\nNOTE the code's own density_floor (1e-8 normalised) is a thousand times BELOW the rtol floor, so it offers no protection.\n\nALSO TO FIX HERE: finding F18 in findings.md presents the mumps1-\u003emumps2 ladder (13.8x) without saying it was measured entirely on RESTARTS (test4steady/test5steady logs say 'Restarting from ID: ...') and without naming the slope limiter. As written the ladder reads as portable to a from-scratch VanAlbada run, where it fails. F18 needs both caveats.","notes":"2026-09-20 measurement, from logs already on disk. The floor is reached in nearly every run, not rarely. When diagnose is on the solver prints its smallest scaling factor per rescale. Across sixteen runs carrying that line, fourteen report exactly 1e-5, which is rtol. That is the fingerprint of the floor: a scaling factor landing precisely on rtol cannot be a genuine variable magnitude. Confirmation from a run that used a different tolerance: test5steady-2026-08-01-va-step3-tolerance reports exactly 1e-6, its own rtol. The two runs that escaped are test2_20.0-21.0ms-2026-09-19 at 5.25e-5 and test5steady-2026-08-01-va-step4-matrixfree at 4.69e-6, both short.\n\nTwo corrections to how this issue has been described. The trigger is not a variable of exactly zero. It is any element whose normalised magnitude falls below rtol, and exact zero is only the extreme case. And the non-zero neutral initial conditions do not remove the exposure: densities and temperatures are floored positive by the physics and stay clear of 1e-5, but momenta are signed, pass through zero at every stagnation point, and nothing floors them.\n\nStill unmeasured: what share of the residual norm the floored elements actually carry. The diagnostic proves the floor activates, not that it dominates. See the follow-up issue.","status":"open","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-08-25T11:18:37Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:45:10Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-pp3.1","title":"Why is test5 so much more expensive than test4 for CVODE, now that flashes are acquitted?","description":"Opened from the cf side on 2026-08-04 after their campaign verdict (their report output/01_test5_flash_deep_dive.pdf p22-24): the flashes are integrator-noise readouts, computationally free on both test5 variants, so they do not explain test5's cost.\n\nFacts that reset the question's premises (checked against the grids and restart seeds on disk):\n- The grids are NOT the same machine: test4 is nx=40 on R 0.15-0.78 m; test5 is nx=20 (half the radial cells) on R 0.27-1.48 m.\n- test5's plasma is HOTTER and THINNER, not colder, at least in the restart seeds: rank-0 max Te ~ 1120 eV at Ne ~ 1.7e18 m^-3 on test5 vs ~ 16 eV at up to 2.2e20 m^-3 on test4 (same Nnorm). This matches the earlier separatrix conduction-stiffness ranking (test5steady ~ 1300x test4stiff).\n- BUT the obvious culprit is already partly acquitted: capping electron parallel conductivity by 45-150x on test5steady left total CVODE work within 2% (cf ladder, 2026-08-04). So core electron conduction stiffness does not set CVODE's pace there either.\n\nThe sharp open question: what actually paces CVODE on test5 - neutral equations, SOL/target stiffness, ion channels, the preconditioner's coverage, or per-step overheads? Candidate approach: per-equation residual/timescale census on the retained cf dumps (they keep dumps; this project's bundles do not), plus a CVODE order/step-size history comparison test4 vs test5 at matched tolerance. The capped test5stiff confirming run (cf side, running now) adds another datapoint.\n\nRuns happen where the case lives; the cf dumps under /home/mike/work/cases/perftests/core-flashes are retained and usable read-only.","acceptance_criteria":"A named pace-setter for CVODE on test5, with evidence that removing or relaxing it moves the cost, and a test4-vs-test5 accounting that survives detrending.","notes":"CORRECTIONS AND NEW FACTS 2026-08-04, from the user and from re-checking with proper normalisation (test4 Tnorm=100, test5 Tnorm=1 - the original issue text compared raw numbers and wrongly called test5 hotter):\n- Lineage (user): test5 is the low-resolution version of the large-machine case, as test2 is of test4. The high-resolution cousin of test5 was abandoned for extreme slowdown. Performance anchors: test4scratch now ~30 min on SNES-MUMPS-2; test5, at a quarter of the resolution, takes 10-30 h on CVODE and longer on SNES with mass nonlinear failures.\n- Corrected temperatures: test4 core ~1600 eV at 2.2e20 m-3; test5 core ~1120 eV at 1.7e18 m-3. The user is right that test5 is colder. BUT the conduction diffusivity proxy T^2.5/n is still ~53x LARGER on test5, because its density is ~130x lower. Colder yet far more collisionless - the density division is what the colder-so-softer intuition misses.\n- New lead from the cap acquittal: the cf ceiling capped ELECTRON conduction only. The ion channel (kappa_par_d+ ~ 8e10 at the test5 core) is comparable to the strongest useful electron cap (1e11) and was untouched, so the acquittal of conduction stiffness is incomplete: below cap ~1e11 the ion channel becomes co-dominant. Cheap discriminator: rerun with BOTH species capped (d+:conduction_kappa_max as well) - if cost still does not move, conduction as a whole is acquitted; if it moves, the pace-setter is the ion channel.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-04T13:34:18Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:30Z","closed_at":"2026-09-18T12:42:30Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-pp3.1","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-04T14:34:18Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-uh6","title":"A crawling rung must record a progress rate, not a hole","description":"Six rungs have now aborted on the 2 h no-output cap. Each carries nl_its and solver_fails but no wall_s, no sim_time_ms, no ncalls and no ms_per_24h, so each is a hole in the history rather than a data point.\n\nIt does not have to be. Every aborted run ran for a known wall duration and reached a known simulated time - a35e2f5e got to 0.350 of 20 ms in two hours - and ms_per_24h is already a column in the index. Filling it on the abort path converts the six existing holes into six comparable measurements, and they are already comparable to the completed rungs because that column is what the completed ones report too.\n\nThis was a nuisance for a retrospective ladder. For the standing watch it is close to fatal: the regressions most worth catching are the ones that make a run too slow to finish, so they will abort rather than return a slow number. An abort that records nothing says only that something happened.\n\nScope: the abort path in the run tooling and the extractor, so that outcome=crawl_aborted still fills wall_s, sim_time_ms and ms_per_24h from what the run achieved before it was killed. Backfill the six existing rows from their logs.\n\nRelated but distinct from so-oze, which is about a build that never ran.","acceptance_criteria":"The six existing crawl_aborted rows carry a progress rate, and a newly aborted rung fills it without hand editing.","status":"open","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T11:37:29Z","created_by":"Mike Kryjak","updated_at":"2026-08-01T11:37:29Z","dependencies":[{"issue_id":"so-uh6","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-08-01T12:37:38Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-1cn","title":"Per-merge trigger and the regression rule for the standing watch","description":"perf-bisect became a standing watch on 2026-08-01: it gains a rung as each PR lands on master, rather than being a one-off backward ladder. That needs two things the current design does not have.\n\nA trigger. What fires a rung when a PR merges, how the commit is picked up, and what happens when several land close together. Batching is probably right - a rung per day of merges rather than per merge - but that is a decision, not an assumption.\n\nA regression rule that survives uncontrolled loading. A watch that fires whenever a PR lands cannot choose its slot or its neighbours, so it must gate on work counts, not wall clock. F17 settled that already: wall clock carries a tens-of-percent communications term, F22 puts the concurrency penalty at 10%, and the first ladder pass demonstrates it directly - nonlinear iterations across the five measured anchors were 4541, 4542, 4563, 4546, 4541, inside 0.5%, while wall time spanned 2409-2751 s and tracked launch order rather than commit age. Counts were the signal; wall clock was noise.\n\nSo the rule gates on nl_its and ncalls against the previous rung, with a threshold set from the 1D case own repeat scatter once that exists (so-v3o). Do not reuse test4steady 2 percent figure - it is a different case.\n\nDepends on the 1D test, because the per-merge cadence is only affordable there.","acceptance_criteria":"A written trigger and threshold rule, and a rung that fires from a merge without being launched by hand.","status":"open","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T11:37:28Z","created_by":"Mike Kryjak","updated_at":"2026-08-01T11:37:28Z","dependencies":[{"issue_id":"so-1cn","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-08-01T12:37:55Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-1cn","depends_on_id":"so-v3o","type":"blocks","created_at":"2026-08-01T12:37:56Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-5rh","title":"Turn on diagnose_failures and name the 661 failures","description":"The cheapest missing measurement in the whole project, and every run so far has had it off.\n\nsolver:diagnose_failures = 0 on all four short-test runs. PETSc records the reason for each failure and snes.cxx:812 will print it along with the min/max of every field and its ddt. Line search collapse, iteration limit, and NaN point at three completely different fixes, and nothing currently distinguishes them.\n\nAffordable: solver_fails_max was 7 against a cap of 20 on test5steady, so the run has plenty of headroom to carry the diagnostics. Cost is log volume only.\n\nDo this before or alongside the other experiments here - without it, a failure count that moves cannot be attributed.","acceptance_criteria":"test5steady rerun with diagnose_failures = true, and a breakdown of the failures by PETSc reason.","notes":"ANSWERED 2026-08-01 from the CVODE-like scan's baseline, which is the first case in this project ever to set diagnose_failures = true.\n\nFull run, all 731 failures:\n  reason -9  SNES_DIVERGED_DTOL     412  (56%)  residual GREW past the divergence tolerance\n  reason -5  SNES_DIVERGED_MAX_IT   319  (44%)  ran out of the 10 Newton iterations\n\nAn early 39-failure sample read 72% MAX_IT / 28% DTOL. Over the full run it is the other way round, so do not quote the early split.\n\nDTOL is the headline. PETSc's divergence tolerance defaults to 1e4 times the initial residual, so on the majority of failures the Newton step did not merely fail to converge - it made the residual four orders of magnitude worse. That is violent divergence from a single full undamped step, not slow progress.\n\nNothing at -6 SNES_DIVERGED_LINE_SEARCH, and that is structural rather than informative: with snes_linesearch_type = basic there is no line search that can fail. Nothing at -3 linear-solve failure and nothing at -4 NaN either, so the LU and the residual evaluation are both healthy.\n\nConsequence: the problem is purely the length of the Newton step being attempted, and a line search is aimed straight at the dominant mode. This raises so-zk3 above the controller work in expected value, which is not where the analysis had it.\n\nSupporting number from the same run: 399 of 517 accepted steps converged in ONE Newton iteration, so the controller sat at its maximum growth factor for 77% of steps.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:12:58Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:29Z","closed_at":"2026-09-18T12:42:29Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-5rh","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T09:13:08Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-8b8","title":"Matrix-free Newton operator with a hard-lagged Jacobian (F13)","description":"The structural fix for test5 cost, and it attacks the 84 percent.\n\nPETSc split on test5steady SNES-MUMPS-2: finite-difference colouring 84.0%, LU numeric factorisation 11.3%, Krylov solve 1.7%, genuine residual evaluations 1.8%. The LU is already perfect - lin_its equals nl_its in every one of the 470 accepted steps, one Krylov iteration per Newton iteration. There is no preconditioner improvement available. All the cost is BUILDING the operator: 2406 Jacobian builds at about 160 residual evaluations each, 421 residual evaluations per Newton iteration.\n\nThis is also the answer to why CVODE stays within 2x with only a conduction preconditioner: it never builds a Jacobian.\n\nThe change: matrix_free_operator = true with lag_jacobian raised to 20-50. The Newton operator becomes an exact directional derivative at one residual evaluation per Krylov vector; the assembled LU-factorised Jacobian is demoted to preconditioner only, so it can be lagged hard without hurting Newton convergence. That is CVODE architecture with a much better preconditioner.\n\nArithmetic against the current 385k evaluations: 24 builds at lag_jacobian=20 is 3.8k, plus 913 Newton iterations at even 20 Krylov vectors each is 18k. Large enough margin to absorb being badly wrong about the Krylov count.\n\nNot a new design - SNES-1 and SNES-PTC-1 already run matrix_free_operator = true. Prepare it as a MUMPS variant.\n\nRisk to watch: directional derivatives on a non-smooth residual (van Albada limiter, floors, softFloor) can be noisy in the differencing parameter. The FD-coloured Jacobian is equally inconsistent there, so this is not obviously worse, but watch lin_its per Newton iteration - if it explodes, the lagged preconditioner is the problem, not the operator.","acceptance_criteria":"Measured on test5steady against the SNES-MUMPS-2 row: Jacobian builds, RHS evaluations, wall, and lin_its per nl_it. Physics compared against the same baseline.","notes":"MEASURED 2026-08-01 on test5steady, and the recommendation in this issue is WRONG for this configuration. Matrix-free loses badly, and the reason is the very thing this issue cited as the opportunity.\n\n                          wall    ncalls   nl_its   lin_its  fails   jac   t_jac  t_ksp\n  step2 assembled lag3     208 s   133986    1204      1204     96    803   0.805  0.014\n  matrix-free lag5        2038 s  1005985    2351    107924   1226   3528   0.362  0.570\n  matrix-free lag20 *     2410 s  1203930    5813    216471   1070   4189   0.363  0.569\n  * also carried the tightened tolerance, so confounded; lag5 is the clean test.\n\nThe architecture change did exactly what was predicted: Jacobian construction fell from 0.805 to 0.362 of the run. It still lost by 10x, because Krylov replaced it at 0.570.\n\nThe mechanism, and it is the flaw in the original reasoning. With the assembled Jacobian and exact MUMPS LU, lin_its EQUALS nl_its - 1204 and 1204, one Krylov iteration per Newton iteration. That is the theoretical optimum for a linear solve and it cannot be improved on. The LU is an exact inverse of the ASSEMBLED Jacobian; the moment the operator becomes the true matrix-free Jacobian, the preconditioner stops being exact and GMRES has to iterate - 46 iterations per Newton iteration at lag 5, each costing a full residual evaluation. Lagging harder makes the preconditioner staler and it gets worse, not better.\n\nThis issue reasoned that 84% in Jacobian construction was the opportunity and the 1.7% Krylov share was proof preconditioning had nothing left to give. Both halves were right and the conclusion drawn from them was wrong: the 1.7% is small BECAUSE the Jacobian is assembled and exactly factorised. The cost and the optimality are the same fact, and you cannot keep one without the other.\n\nThere is also a secondary cascade: matrix-free raised failures to 1226, and a failure forces a Jacobian rebuild and resets the lag (snes.cxx:857), so builds went UP to 3528 despite lag 5 against lag 3.\n\nWhat survives. The lever is to build the assembled Jacobian LESS OFTEN, not to stop building it - which F18 already showed is safe (lag 1 to 3 on test4steady, -58% with an identical trajectory). Branch C is running that sweep now at lag 5, 10 and 20 on the best configuration.\n\nThe test4steady isolation described in the original issue is now moot for matrix-free; retarget it at the assembled lag sweep if a clean isolation is still wanted.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:12:58Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:29Z","closed_at":"2026-09-18T12:42:29Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-8b8","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T09:13:07Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-pp3","title":"Flash-driven solver cost on test5","description":"The perftest-side workstream on the core flashes: what they cost the solver on test5, and what removes that cost. Distinct from the cf tracker at notebooks/ai-diagnose/core-flashes, which owns the physics diagnosis on the st40 cases. Read that projects investigation_notes.md before starting anything here.\n\nWhy this epic exists. Analysis on 2026-08-01 tied the test5 SNES failures to the flash mechanism diagnosed in the cf project: electron parallel conduction acting on derived Te = Pe/Ne, with kappa_par_e enormous in hot weakly collisional cells, converting sub-resolution Te corrugations into large ddt(Pe) sources.\n\nWhat the store data supports:\n- Ranking the four short tests by T^2.5/n at the outboard separatrix gives test4stiff 1x (0 failures), test4steady 11x (0), test5steady 1300x (661), test5stiff 28000x (245). The two failure-free tests are 100-3000x less stiff in electron conduction.\n- test5 runs with the electron conduction flux limiter OFF (e:kappa_limit_alpha = -1); both test4s have it at 0.2. That limiter is the only cap on kappa_par (braginskii_conduction.cxx:252).\n- On the identical test5steady case, SNES carries max|ddt(Pe)| 11x larger than CVODE and 5x more localised (max/rms 18.1 against 3.8).\n- Loosening atol/rtol raises the fluctuations 35x on peak and 16x on RMS, measured controlled on test4steady across the one tolerance step of the MUMPS-1 to MUMPS-2 scan.\n- The flashes cost CVODE its timestep directly: Spearman 0.89 between max|ddt(Pe)| and RHS evaluations per output interval on test5stiff. They do not time SNES failures (Spearman -0.37 and 0.07), because SNES has no time error control. The flash sets how high the wall is; the timestep controller decides how often SNES walks into it.\n\nTwo kinds of work hang off this, and they should not be confused:\n- Make SNES survive the flash: matrix-free operator, controller, tolerance, line search. These do not change the answer.\n- Establish that the flash is what sets the ceiling: cap kappa, turn the limiter on. These DO change the answer, so they are diagnostics, not speedups.\n\nRuled out already, do not reopen without new evidence: limiting at cell centres rather than faces (test5 has no limiter and is the case that fails); conduction reconstruction (ProductJK and Harmonic both tried, no performance change - they change how kappa is averaged to faces, not how large it is).","notes":"SCAN COMPLETE 2026-08-01. Nine runs on test5steady, one build, sequential and alone. Result: two settings on SNES-MUMPS-2 give 3.25x, and SNES is 6.0x faster than CVODE-2 on the test where it was believed to lose.\n\n  target_its = 5              (was 100, against max_nonlinear_iterations = 10)\n  snes_linesearch_type = bt   (was basic)\n  676 s -\u003e 208 s, CVODE-2 1249 s, peak target Te moves 0.002%\n\nCumulative ladder: baseline 676, +target_its 432, +bt 208, +tight tolerance 2071, +matrix-free 2410.\nLag sweep on the best configuration: lag 3/5/10/20 = 208/212/241/250 s.\n\nThree routes at the 84% Jacobian share are measured and closed - tolerance, matrix-free, and lagging - see F29 and the corrected F27. The single most useful correction: the 84% is not a cost with a lever behind it, because lin_its equals nl_its exactly and that optimum exists BECAUSE the Jacobian is assembled and exactly factorised.\n\nOpen, in order: the line-search scan (so-erp), the count-scatter question (so-xi1), and whether these two settings transfer to test4 and to test2.\n\nEverything extracted and validated; no case directories deleted; nothing committed.","status":"closed","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:12:12Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:32Z","closed_at":"2026-09-18T12:42:32Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-v3o","title":"A 1D perftest for history, based on 1D-recycling-dthe","description":"The 2D test cannot measure most of the window. test2_0-20ms completed on only 5 of 12 anchors: everything from the 2026-01 anchor backwards collapses its timestep to ~5e-5 inside the first output interval and is aborted. So the ladder measures five months of a twelve-month history, and the older half is unmeasurable rather than slow.\n\nThe user's proposal, 2026-08-01: build the history test on hermes-3's tests/integrated/1D-recycling instead. Its component list is d+, d, e, sheath_boundary, collisions, recycling, reactions, electron_force_balance, neutral_parallel_diffusion - about as complete as 1D allows - it runs fast, and it is far more stable than 2D. Stability is the point: a test that only runs on the recent half of the window cannot answer how performance evolved across it.\n\nConvenient side effect: the migration ledger was verified against this very input, so the option translations already have evidence behind them.\n\nOpen before this can be specified:\n- Sizing. How much simulated time, and nout, to be long enough to time reliably but cheap enough to run twelve times over.\n- Start mode. From scratch, or from a seed like the 2D tests use.\n- Recipe. SNES-MUMPS-1 for continuity with the 2D ladder, or something suited to 1D.\n- Whether the MC limiter constraint still binds, and whether a 1D case is sensitive to the limiter at all.\n- Whether the older anchors need input migrations beyond the ones already in the ledger.\n\nNot urgent - the user has parked it.","acceptance_criteria":"A named test definition and sizing that runs on all twelve anchors, or a recorded reason why some still cannot.","notes":"UNPARKED and raised to P1 on 2026-08-01. The user extended perf-bisect from a one-off backward ladder to a standing watch that gains a rung as each PR lands on master. That makes this case a prerequisite, not a nice-to-have, for two independent reasons:\n\n- Cost. A 2D rung is 40-45 min when it completes, which does not work at merge cadence. A 1D case does.\n- Coverage. Everything from the 2026-01 anchor backwards collapses its timestep and aborts, so the 2D ladder can only ever measure five months of the twelve-month window.\n\nThe five open questions in the description - sizing, start mode, recipe, whether the MC constraint binds, whether older anchors need further migrations - are now blocking rather than deferred. Answer them first.\n\nLimitation to record while building it: a 1D watch has no perpendicular transport, no topology and no X-point, so it cannot see the flashes or the NVd corrugation. It catches broad regressions cheaply and misses the 2D-specific ones entirely. So it is the per-merge watch, and the 2D case is retained at low cadence - epoch boundaries, or when the 1D watch flags something - rather than being replaced by it.","status":"open","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T06:35:29Z","created_by":"Mike Kryjak","updated_at":"2026-08-01T11:37:27Z","dependencies":[{"issue_id":"so-v3o","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-08-01T07:35:37Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-gkj","title":"perf-bisect: define the test — truncated test2scratch on SNES-MUMPS-1","description":"The steady/stiff tests restart from a fixed base/ state produced by one build; run under an old commit that state is no longer its equilibrium, so timing mixes solver speed with seed drift. A from-scratch run has no seed at all, which removes the confound. test2scratch at nout=450 (45 ms) took 56 m 36 s on 2026-06-12 under SNES-MUMPS-1. Choose an nout that costs about 30 min on today's build, by measuring once - the from-scratch transient is the expensive part, so cost is not linear in nout. Fix nout and simulated duration across the ladder and measure wall time; never fix wall time. SNES-MUMPS-1 is the recipe: the user confirms it has been in play across the whole window. Open question: is one recipe enough, or does a CVODE arm belong here too, given a change could help one solver and hurt the other?","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:44:23Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T12:18:24Z","closed_at":"2026-07-31T12:18:24Z","close_reason":"Settled 2026-07-31. The test is test2scratch at nout = 200, SNES-MUMPS-1, MC build, slot 1.\n\nThe user chose 20 ms of simulated time rather than a wall-clock target, which is the better axis anyway: the work is fixed and the wall time is the measurement. The input steps at 95788 * 0.1, i.e. 0.1 ms per output, so today's nout = 450 is 45 ms and nout = 200 is 20 ms. No calibration run is needed, which also sidesteps having to calibrate on a VanAlbada build for an MC ladder.\n\nExpect appreciably more than 20/45 of the full 56 m 36 s, because the from-scratch transient is front-loaded rather than uniform. From scratch also means no seed state, so the restart-drift confound that rules out the steady and stiff tests does not apply here.\n\nOne recipe, SNES-MUMPS-1: the user confirmed it has been in play across the whole window. A second CVODE arm remains possible - a change can help one solver and hurt the other - but is not being taken now.","dependencies":[{"issue_id":"so-gkj","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:12Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-wvw","title":"perf-bisect: does the oldest anchor still build? (window feasibility)","description":"Everything else in perf-bisect depends on how far back the ladder can reach, so probe before designing. hermes-3 pins BOUT++ as a submodule (bumped 10 times in the last 12 months), so each commit carries its own BOUT++; PETSc does not follow, and comes from the spack hermes3 env at 3.23.3. A year-old BOUT++ against PETSc 3.23.3 is the most likely build failure. Take the oldest candidate anchor, build it in a fresh clone/worktree outside /home/mike/work/hermes-3, and record what happens. Open question: if it fails, is the answer to shorten the window, or to pin an older PETSc per epoch? Shortening is cheaper and probably right, but the failure mode decides.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:44:23Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T11:41:51Z","closed_at":"2026-07-31T11:41:51Z","close_reason":"Probed 2026-07-31. The 12-month window is feasible: the oldest anchor a35e2f5e (2025-08-27), carrying BOUT++ c4c149a2 (2025-08-13), builds and links against spack PETSc 3.23.3. FindPETSc's MULTIPASS_TEST_3 succeeds and CMake reports PETSc support: ON. Warnings-as-errors was also a non-issue: 7 warnings, all in BOUT-dev headers, none fatal with HERMES_ERROR_ON_WARNINGS=ON.\n\nThe build did fail first time, but not for either reason expected. HERMES_SLOPE_LIMITER=VanAlbada is not buildable at that commit: FV::VanAlbada entered BOUT++ on 2026-06-11, so it exists for only the last two of the twelve anchors. FV::MC has existed since 2017. Rebuilt with -s MC: clean, hermes-3 executable produced, 0 errors. See the ladder issue for the consequence.\n\nNote the failure shape for the driver: CMake accepted the VanAlbada option and generated a config header naming a type that did not exist, so it failed at 82% of the compile, not at configure. Checking configure output alone would have called it a success.","dependencies":[{"issue_id":"so-wvw","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:11Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"so-2mf","title":"Campaign claims store-independence but still loads case directories","description":"analysis-solver-opt.py's docstring says this campaign reads the results store and never a dump, which is the whole reason it exists separately - so its reports keep building after R8 deletes the run directories. The campaign-local pages do read bundles. But Campaign.run_cases still resolves every study case against case_root and raises MissingCase if the directory is gone.\n\nHit 2026-07-30: the six R20 noise runs were extracted, validated and deleted as R22 licenses. A study referencing one of them then failed to build. The store has everything that study needed; only the loader disagreed.\n\nSo the promise is not kept, and the failure mode is bad - it appears only once a directory is deleted, which is exactly when the store is supposed to take over, and it makes deleting runs unsafe in practice while R22 says it is safe.\n\nNeeds a decision on which pages genuinely require dump data. If some do, the campaign should load bundles for the rest and say clearly which pages cannot be drawn from a bundle. If none do, the loader should read the store.","acceptance_criteria":"A study referencing only extracted-and-deleted runs builds its PDF from the store alone, or the campaign states per page which need dumps and skips them with a visible note.","status":"closed","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T18:53:34Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T23:02:29Z","closed_at":"2026-07-30T23:02:29Z","close_reason":"Fixed 2026-07-31. CaseLoader gained an optional dir_fallback: a callable name -\u003e path, tried only when no directory exists under case_root. dir() prefers the live case directory and falls back to it; missing() counts a case missing only when neither resolves; case() distinguishes the two failures, so asking for a dataset on a record-only case says the record holds text and not datasets rather than 'directory not found'. Campaign gained needs_dumps (default True, so the physics campaign is untouched) which skips the eager pre-load that opened every dataset for nothing.\n\ncase_comparison learned nothing about the perftest store: the fallback is a plain callable and the solver-opt campaign supplies bundle_for(), keeping campaign-specific knowledge in the campaign file.\n\nVerified: study 2 builds with no dataset loads at all, using a run whose directory was deleted; study 1 rebuilds; the physics campaign's study 3 rebuilds and still loads dumps as before; and all four resolution paths were exercised directly, including both error messages.","dependencies":[{"issue_id":"so-2mf","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-30T19:53:40Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-0a6","title":"Residual conclusions before 2026-07-30 were guard-cell garbage (F20)","description":"The resid_* fields hold up to 1e15 in the radial guard cells against 1e-3 interior. Every residual share computed before the extractor fix summed them in, so which equation 'holds the solver up' was answered by whose guard cells were worst: Pe 95% contaminated, versus Pd 74% / Pe 26% interior-only, and NVd/NVd+ rather than Pe on the scale_vars runs.\n\nThe six test4steady runs are re-extracted and correct. What is NOT checked: every other analysis that read these fields, including the residual_norm_postprocessing showcase notebook, which uses the raw sum and whose published conclusions are therefore suspect. Also unchecked: whether snes_local_residual has the same problem, and whether BOUT++ should be zeroing those cells at source rather than every reader trimming them.\n\nRelates to the neutral-pressure-spike issue: with guards excluded, Pd genuinely does dominate the MUMPS-1 runs.","notes":"Showcase notebook checked 2026-07-31: its conclusions are SAFE. Shares are identical raw versus masked to the digit on both its cases — MC Pe 86.32%/Pd 13.46%, VanAlbada Pe 72.03%/Pd 26.97% — because the guard cells on those two runs hold 5.47e-310, a denormal that underflows to zero when squared. It was right by luck rather than by reasoning: freshly-allocated pages come from the OS zeroed, and only reused memory carries garbage, which is why the same field is clean on one run and 1e15 on another.\n\nIts reduction code has been corrected to mask explicitly via clear_guards so it no longer rests on that. The edit could NOT be executed: the notebook imports a helper module (perftests/analysis.py) that no longer exists and was never in git. Filed separately. Stored outputs left in place and remain valid, since the numbers are provably unchanged.\n\nTrap written into hermes-oracle/docs/postprocessing.md, next to the other guard-cell traps, including the two sub-traps: which fields are affected varies run to run, and this applies only to the resid_ diagnostics, not to evolved fields whose guards carry boundary conditions.\n\nDecided: no BOUT++ source fix. The extractor already masks and refuses to proceed without a mask, and R8 deletes the dumps, so the exposure is bounded to ad-hoc readers — which the doc note now covers.","status":"closed","priority":1,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T14:59:59Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T09:42:11Z","closed_at":"2026-07-31T09:42:11Z","close_reason":"All three open items resolved.\n\nsnes_local_residual is clean, and so is snes_global_residual: the resid_ fields use emptyFrom() (snes.cxx:391-394) which allocates without initialising, while local_residual is explicitly zeroed (snes.cxx:448). Measured guard max exactly 0.0 against 7.6e6 for resid_Pe. So resid_final, resid_drop and resid_per_rhs in every index row are sound.\n\nEvery other reader enumerated: five files reference resid_. Three read bundle tables or column names only (perftest/index.py, perftest/store.py, analysis-solver-opt.py) and so consume already-masked data. Two notebooks read raw dump fields, and both are unaffected — residual_norm_postprocessing (MC Pe 86.32/Pd 13.46, VanAlbada 72.03/26.97) and scale_vars_residual_weighting (no-scale_vars Pe 86.2/Pd 13.7, scale_vars Pe 57.2/NVd+ 19.5/Pd 19.1), identical raw and masked in both. Their cases hold denormals (~5.5e-310) in the guards rather than garbage, because freshly-allocated pages arrive from the OS zeroed.\n\nNo BOUT++ source fix, by decision. The extractor masks and refuses to run without a mask; R8 deletes the dumps; the trap is documented in hermes-oracle/docs/postprocessing.md for ad-hoc readers.\n\nLoose end, not blocking: scale_vars_residual_weighting.ipynb still asserts 'Guard cells are checked to be zero below, so summing the full array is safe' and checks only resid_Pe using index slicing rather than clear_guards. Its numbers are right today and its reasoning is not. It is untracked in-progress work, so it was left alone rather than edited.","dependencies":[{"issue_id":"so-0a6","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T16:00:06Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-j6v","title":"First results table and plots (R11)","description":"The user wants to see the results as a table, and some first plots, before any solver physics work. R11 outranks everything else: if a result cannot be presented digestibly it is not finished.\n\nRead the index only, never case directories or dumps. Analysis belongs in the campaign script per the perftests conventions, with anything campaign-specific in a local page function rather than edited into the shared case_comparison package.\n\nHonest scope, and this must be stated on the output: these are descriptive. No difference shown can yet be called an improvement, because the noise floor has not been measured. Show what was run, what it cost and where the time went — not which recipe wins.","acceptance_criteria":"A table of recorded runs plus first plots, produced from the index alone, carrying the caveat that no difference is yet established as real.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T10:38:17Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T13:04:19Z","closed_at":"2026-07-30T13:04:19Z","close_reason":"Delivered as a study rather than a loose table: analysis-perftests.py now carries test4_mumps1_to_mumps2, emitting output/05_test4_mumps1_to_mumps2.pdf. Four pages — provenance cover, cost-per-step table, per-run evolution histories, and the log_view time breakdown.\n\nScope changed from the original description in two ways, both for the better. It reads the BUNDLES, not just the index, because 'how things evolved' needs the per-step histories and those are what survive R8 deletion. And it no longer needs the old on-disk runs, which the user is discarding, so the blocker on bulk extraction was stale and removed.\n\nThe plotting pages are campaign-local in analysis-perftests.py per the conventions; the store reader is general and went to sdtools as perftest/store.py.","dependencies":[{"issue_id":"so-j6v","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-30T11:38:34Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-0xw","title":"Extract every run still on disk into the store","description":"About 30 case directories sit in test4dev and test5dev, roughly 52 GB. They are the only runs that exist, and several are the evidence behind current findings. Extracting them populates the index with real rows so the first table and plots have something to describe.\n\nNot all will extract cleanly and that is the point: runs launched before the tee convention have no console, so no log_view, no options_left and no PETSc version. Those rows come out thinner and the report says which. Record what is there, flag what is missing, do not invent.\n\nOrder matters: run after the extractor has tests, since this writes many rows at once and a silent bug would be copied across all of them. Dumps still exist so a re-extraction is possible, which is what makes this recoverable.","acceptance_criteria":"Every case directory on disk is either extracted with a validated report, or listed with the reason it could not be.","notes":"Open question for the next session, 2026-07-30: the user said they would rather start from scratch and discard the old runs than extract them. If that stands, this issue should be CANCELLED rather than done — the 18 legacy rows already preserve those runs' wall times in the index, and extracting the dumps would only add cost breakdowns for runs nobody intends to compare against again.\n\nWhat is NOT recoverable if the dumps go: ncalls, the time breakdown, residuals and physics for those runs. The index keeps wall time only. The long test5 scratch runs are 6-34 h each and cannot be casually remade; the short ones can.\n\nSix test4steady runs from 2026-07-30 ARE extracted and validated (state=recorded), so those case directories are safe to delete whenever the space is wanted — about 2.5 GB of the 55 GB in cases/perftests.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T10:38:16Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T10:03:41Z","closed_at":"2026-07-31T10:03:41Z","close_reason":"CANCELLED 2026-07-31 by the user's decision: runs made before this project are separate work, to be redone and deleted, and are never to be extracted. The 43 unextracted directories in test2dev/test4dev/test5dev are not a backlog — they are not this project's runs.\n\nReplaced by the standing rule that the index defines what exists: a run is part of this project iff it has an index row, and no tool or agent should enumerate case directories to find work. BOUT.log.console is the physical marker, since it exists only for runs launched under the tee convention — 22 directories have one, 41 do not, and all 19 project rows carry one.","dependencies":[{"issue_id":"so-0xw","depends_on_id":"so-219","type":"blocks","created_at":"2026-07-30T11:38:35Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-0xw","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T11:38:33Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-0xw","depends_on_id":"so-x78","type":"blocks","created_at":"2026-07-30T11:38:34Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-x78","title":"Create the index and migrate the 18 legacy rows (R6/R7)","description":"The store has a README, a schema and an empty runs/. It has no index, so the extractor's first write would create one and the real results still live in the legacy 14-column run_records.csv. Two ledgers alive at once is the temporary state the project CLAUDE.md warns about, and it ends here.\n\nMigrate the 18 legacy rows into the new schema rather than archiving them: they are the evidence behind F1, F2 and F5. Fields the old rows cannot supply (RHS counts, cores, CHECK level, slot, outcome, epoch) are recorded empty — never guessed, since empty means unknown and a guess becomes a conclusion nobody re-checks.\n\nColumn order is currently defined in sdtools perftest/index.py INDEX_COLUMNS, 51 columns, meaning-first. Check it against schema.md and make one of them authoritative.","acceptance_criteria":"index.tsv exists in the store with the 18 legacy rows migrated, no invented values, and run_records.csv marked superseded.","status":"closed","priority":1,"issue_type":"task","assignee":"Mike Kryjak","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T10:37:53Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T10:46:00Z","started_at":"2026-07-30T10:41:40Z","closed_at":"2026-07-30T10:46:00Z","close_reason":"index.tsv exists in the store with all 18 legacy rows migrated: 51 columns, 14 populated, 37 empty and correctly so. Nothing guessed — run_time_str dropped as a duplicate, hermes_commit_date as recoverable from the commit. run_records.csv marked superseded in both CLAUDE.md files and takes no new rows; recording instructions now point at extract_test.py.\n\nThree schema gaps found and documented: state gains 'legacy' (no bundle, never validated, must not read as a measurement of the same standard); test_id is not unique because two runs of one test can start in the same second in different slots, so the later takes a -2 suffix; concurrency left empty on legacy rows because the old ledger may not list every concurrent run, making any count a lower bound. perftest/index.py INDEX_COLUMNS declared the authoritative column list.\n\nByproduct worth more than the migration: F15 — the three June baselines all ran concurrently.","dependencies":[{"issue_id":"so-x78","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T11:38:32Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-s0b.1","title":"Build the record extractor","description":"Missing piece: nothing yet turns a finished case directory into a record. Reads a case dir, writes the per-test bundle into perftest-results/runs/\u003ctest_id\u003e/ and appends one row to the index per schema.md, then reports whether the extraction validated so the directory can be deleted (R22).\n\nLives in sdtools, not the results repo and not the read-only hermes-perftest template — it is useful to any Hermes-3 performance work.\n\nInputs: BOUT.inp, BOUT.settings, BOUT.log.0, the captured console (log_view, options_left, snes_view, ksp_view, petsc_version), the dumps for RunMetrics and solver counters, and the resid_ fields for the derived metrics.\n\nReuse rather than rewrite: hermes3/logparse.py already parses log_view tables; cmonitor.py:279-318 already parses the SNES log lines; case_comparison/provenance.py already extracts commits and merged options. Read dumps through xhermes, never raw xbout/xarray.\n\nNeeds the user's explicit go-ahead before any code is written.","acceptance_criteria":"A finished case directory becomes a validated bundle plus one index row, with a clear pass/fail on whether the directory is safe to delete.","notes":"Design settled 2026-07-29. The index is declared before the run, not written after it: a row carries six intent fields (case_dir, test, recipe, varied, epoch, note) and the extractor fills the rest, joining on case_dir with at most one open row per directory. seed, slot and concurrency are measured, not declared. Bundle holds raw files plus three parsed tables (per output step, per PETSc event, residual metrics) so analysis never parses text. See perftest-results/schema.md for the lifecycle and rules. Residual metric design (so-r53.6) does not block: extractor ships without it and is re-run on bundles later, at the cost of losing per-equation fields for any run extracted first.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T14:50:01Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T09:08:12Z","closed_at":"2026-07-30T09:08:12Z","close_reason":"Built and validated in sdtools: perftest/ (extract.py, index.py, recipe.py) plus cli/extract_test.py, with the text parsing in hermes3/logparse.py. A finished case directory becomes a bundle plus one filled index row, and the report's pass/fail is what licenses deleting the dumps.\n\nVerified on real runs: 39 of 51 columns filled on a completed test4steady, and the log_view time shares match F13 independently (jac 0.775, pcsetup 0.151, ksp 0.0095, func 0.0071). A failed run extracts too, classified snes_failure from the '======== SNES failed =========' marker, with sim_time_ms 0.0 recording that it got nowhere. Console trimming drops repeated PETSc object dumps (681 on one run, including 324 PC Object blocks that a fixed SNES/KSP marker list missed), taking the bundle from 2.2 MB to 244 KB and matching the store's stated budget while leaving log_view, options_left and the first block of each object type intact.\n\nThree schema columns are computed that were not in the original plan: hermes_branch from the sdrun banner (better than asking git which branches contain a commit), diffs by comparing the case's [solver]/[petsc] sections against the named recipe, and concurrency as a pass over the whole index after each extraction, since a run that overlapped this one may be recorded later.\n\nLeft empty deliberately: ne_sep/te_sep, which need a separatrix location convention this being a connected double null with four midplanes (so-joa); and the finer outcome classes beyond completed/snes_failure, which is schema.md's own open question.","dependencies":[{"issue_id":"so-s0b.1","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-29T15:50:00Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":8,"comment_count":0}
{"_type":"issue","id":"so-r53.7","title":"Measure log_view overhead, resid_ disk cost, options_left behaviour","description":"Three numbers the capture set needs before it goes on every run. (1) Does log_view slow a run? PETSc accumulates timings whether or not it prints, so the marginal cost should be near zero, but it goes on every run so measure once rather than assume. (2) Disk cost of the seven resid_ fields per output step on a long run — transient since extraction precedes deletion, but three runs share the machine so peak disk matters. (3) Does options_left actually flag unused options on the installed PETSc version? It matters because [petsc] has no whitelist, so a misspelled option is silently ignored and a recipe can quietly not do what it says. All three are cheap: short tests, and (3) can be provoked deliberately with a nonsense option.","acceptance_criteria":"Three measured numbers or behaviours, recorded in findings, with the capture set updated if any surprises.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T14:10:47Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T16:48:01Z","closed_at":"2026-07-29T16:48:01Z","close_reason":"All three measured on nine test4steady runs. (1) log_view and the other PETSc options cost 1-2 s on a 155 s run, inside the 2 s spread between identical controls — free. (2) resid_ fields cost 28 MB per 90 output steps, 393 to 421 MB, about 7%. (3) options_left works and immediately flagged snes_fd_color_use_mat, set in three recipes, as never used (F12). Bonus: 1.3% run-to-run spread at single loading, and PETSc 3.23.3 readable from the console.","dependencies":[{"issue_id":"so-r53.7","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T15:10:46Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-yjs","title":"Solver behaviour puzzles","description":"Investigations into why the solvers behave as they do, as opposed to tuning settings. Findings here feed the optimisation rather than being optimisation themselves. Deliverables are hermes-oracle reports (reports/ directory convention), not just issue comments.","status":"open","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:52:52Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T13:52:52Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-r53.4","title":"Phase 3: decide the capture set (R9)","description":"Decisions taken 2026-07-29: solver:diagnose=true goes on every run (it gates both the SNES iteration counts in the log and the resid_ fields in the dump). Scope is SNES only for now (R26). Instrumentation overhead is accepted — relative improvements remain valid — with one caveat to respect when choosing options: prefer diagnostics whose cost does not scale with the quantity being measured, since a per-KSP-iteration monitor costs more on exactly the recipes that take more iterations and would bias the comparison. Promoting SNES counts into dump variables is wanted but explicitly must not block: first establish what can be extracted from the console and log alone, then judge whether the C++ change is worth it. Remaining work: write the core set and the extended set, and decide which PETSc monitor options are affordable.","acceptance_criteria":"A written core set and extended set, each item justified, with the console-and-log-only route established first.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:25:26Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T08:17:18Z","closed_at":"2026-07-30T08:17:18Z","close_reason":"Capture set agreed 2026-07-30 and written up in capture-set.md as the spec the extractor is built against. Core set, extended set and not-captured list all justified; console-and-log-only route established first (SNES counts stay a non-blocking wish). Both open questions settled: disk cost measured at full scratch length (6.6% of the dump, 0.4 GiB across three concurrent runs, against 79 GB free) rather than extrapolated, and bundle tables are TSV at full float precision, matching the index schema.","dependencies":[{"issue_id":"so-r53.4","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T14:25:26Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-r53.3","title":"Capture stdout at launch so log_view reports survive (F6)","description":"PETSc writes its log_view profiling report to stdout at finalize; sdrun.py ends in execvp so stdout goes wherever the shell sent it, and nothing captures it. Zero reports exist on disk despite log_view being active in three recipes. Fix is redirection at launch plus a naming convention so the report sits beside the run, then wire hermes3/logparse.py to read it. No C++ needed. Belongs in the run lifecycle skill (R22).","acceptance_criteria":"A launched run leaves a parseable log_view report in its case directory, and logparse.py reads it.","notes":"Proposed mechanism. Runs launch from $cases/runplanN.sh in screens h1-h3 as 'echo y | sdrun.py -s=N -p=exe -d case'; sdrun execs into mpirun so stdout goes to the screen and is lost.\n\nChange: append '2\u003e\u00261 | tee \u003ccase\u003e/BOUT.log.console' to each runplan line.\n\nWhy that filename: reset_test.py deletes BOUT.dmp.*, BOUT.log.* and *.pid (reset_test.py:29). Naming it BOUT.log.console means the existing reset sweeps it automatically, so a stale console file cannot survive into the next test in a reused directory. It also avoids the numbered-log parsers, which match BOUT.log.\\d+ and will ignore it. Minor side effect: caseplan's n_logs count gains one.\n\nWhy tee rather than plain redirection: keeps the live view in the screen, which matters for monitoring. This is a stdout pipe and is categorically different from the 'yes | runplan' stdin incident of 2026-07-20 that OOM-killed a run — that filled mpirun's stdin buffer without bound; this writes mpirun's stdout to a file. Worth stating explicitly in the skill so nobody conflates them.\n\nKnown downside to verify: piping makes stdout a pipe rather than a tty, so libc block-buffers instead of line-buffering and the live screen view becomes chunky. Mitigate with 'stdbuf -oL' if it proves annoying. sdrun.py's only tty check is cosmetic (Style(sys.stdout.isatty()), sdrun.py:250).","status":"closed","priority":1,"issue_type":"task","assignee":"Mike Kryjak","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:25:25Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T09:08:04Z","started_at":"2026-07-30T08:42:52Z","closed_at":"2026-07-30T09:08:04Z","close_reason":"Done. The tee-at-launch convention is in the run-case skill as a required step (BOUT.log.console, with the note that piping stdout is not the yes-into-stdin failure). logparse.py now reads it: parse_petsc_logview accepts a case directory, and the parser had to be rewritten — its terminator 'Memory usage is given in bytes:' does not exist in PETSc 3.23.3 output, so it read past the timing table into the object table and died with IndexError. Rows are now split from the right by the 20 known columns instead of hardcoded character widths, which also fixes event names containing spaces. Verified on the nine instrumentation runs: 49 events parsed, reproducing every number in F13 independently. Three failure modes now raise distinct errors: no console, console without a report, and unparsed rows.","dependencies":[{"issue_id":"so-r53.3","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T14:25:25Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-r53.2","title":"Phase 2: inventory every available diagnostic signal","description":"Enumerate what can be captured today from three sources that emit at different rates: (a) dump files — solver counters and timings BOUT++ already writes; (b) BOUT.log.0 — per-solver-step output such as SNES iteration counts and convergence reasons, noting it is overwritten on restart; (c) PETSc's options surface — log_view, monitors, converged-reason flags, convergence-history APIs, and how BOUT++ forwards [petsc] options. Deliverable is a table: signal, source, cadence, cost, survives-restart, available-for-SNES-only-or-CVODE-too.","acceptance_criteria":"One table covering all three sources, explicit about cadence and about the SNES/CVODE asymmetry.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:14:59Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T13:25:24Z","closed_at":"2026-07-29T13:25:24Z","close_reason":"Inventory written to diagnostics-inventory.md: dump signals (universal RunMetrics + per-solver), log-only signals, PETSc option surface, gaps against R9, reuse candidates. Three findings logged (F6 discarded log_view, F7 ncalls already available, F8 solver asymmetry).","dependencies":[{"issue_id":"so-r53.2","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T14:14:58Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-r53.1","title":"Phase 1: read the prior art (diagnostics deep-dive)","description":"Before proposing any capture set, understand what already exists and what the project already knows. Read: hermes-oracle/AGENTS.md and docs/postprocessing.md; the residual_norm_postprocessing showcase notebook; how cmonitor parses BOUT.log.0 and which of its monitors are the reusable physics ones (R9 says the physics monitors are reusable and the rest is not fit for purpose — establish exactly where that line falls); record_test.py and test_results.py extraction.","acceptance_criteria":"A short written account of what exists, what is reusable, and what the residual-norm work already solved.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:14:58Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T13:25:23Z","closed_at":"2026-07-29T13:25:23Z","close_reason":"Prior art read. Key finds: xhermes-first policy constrains how the extractor reads dumps; BOUT.log solver parsing is explicitly sanctioned as direct text parsing; the oracle records a prior preferred metric (cumulative Newton iterations at matched simulated time); sdtools already has a log_view parser and a caseplan run-record with tests; residual analysis exists only as notebook cells.","dependencies":[{"issue_id":"so-r53.1","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T14:14:57Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-ll4","title":"Evaluation method","description":"What 'better' means and on which tests: the objective metric (R15), the test ladder (R16), the machine's own variability (R20), and how failures are classified (R19).","status":"open","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T12:34:51Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T12:34:51Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-s0b","title":"Diagnostics and record","description":"What we capture per run and where it lands. Gates optimisation (R10): nothing is tuned before the capture set and the results store exist.","status":"open","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T12:34:50Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T12:34:50Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-cnv","title":"perf-bisect: how Hermes-3 performance evolved across development (F5)","description":"Repeat one fixed test across many Hermes-3 commits to see how performance evolves with development. Kept deliberately separate from solver tuning: different question, different cadence, and it must not consume the tuning budget. Prompt for it: a BOUT++ update coincided with +13% on test5scratch, unproven since the Hermes-3 commit moved at the same time. Feeds R18's epochs by showing which bumps actually cost performance.","notes":"SCOPE CHANGED 2026-08-01: this is no longer a one-off backward ladder. The user extended it to a standing watch that gains a rung as each PR lands on master, plus an extension to a 1D test. Retrospective bisection and forward regression watching are the same question at different cadences, so they stay in this epic - but the watch needs a trigger (so-1cn), an affordable test (so-v3o, now P1 and blocking) and an abort path that records a rate rather than a hole (so-uh6).\n\nHeadline result, 2026-08-01, from the first full ladder pass, and note what it does NOT cover: no major performance regression in Hermes-3 development BETWEEN 2026-02 AND 2026-07. That is five anchors - 2026-02, 03, 04, 06, 07 - out of a twelve-month window. Everything from the 2026-01 anchor backwards collapses its timestep inside the first output interval and aborts, so the older half is unmeasurable rather than measured-and-flat. Do not quote this as 'no regression in Hermes-3 development' without the window.\n\nAcross the five measured anchors the solver work is flat: nonlinear iterations 4541, 4542, 4563, 4546, 4541 and failure counts 1213, 1213, 1224, 1219, 1213, all inside 0.5%. Wall time spans 2409-2751 s but falls monotonically with launch order rather than commit age, and concurrency in the index counts only this store's runs so the flux-limiter load on the other two slots was invisible. Treat the wall-time spread as load until a repeated control says otherwise - and note this is why the standing watch must gate on counts, not wall clock.\n\nSecondary result, and it belongs to the solver line rather than to history: the older rungs fail on the same wall the tuning work is chasing. Six consecutive aborts on 2026-07-31/08-01 with SNES-MUMPS-1, failure counts climbing 1213 -\u003e 1843 -\u003e 1942 -\u003e 2006 -\u003e 2031 -\u003e 2009 as the ladder walked back, all crawling at timestep ~1e-4 and converging on reason 4 (Newton update small relative to the state) rather than on a falling residual. That is stagnation, not convergence, and it is the same controller inversion the solver epics are about.","status":"open","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T12:03:11Z","created_by":"Mike Kryjak","updated_at":"2026-08-01T11:37:57Z","dependencies":[{"issue_id":"so-cnv","depends_on_id":"so-nh3","type":"blocks","created_at":"2026-07-29T13:35:28Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-cnv","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:57Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-q30","title":"Set up the findings log (R24)","description":"R24 calls the findings log the most important artefact the project produces: what was learned and why decisions were taken, including dead ends. Distinct from beads, which tracks what needs doing. Requirements on it: co-authored by the user rather than written at them, curated for high signal-to-noise with entries earning their place, pruned as understanding improves, and doubling as the teaching record under R12. Decide format, location, and the working rhythm for co-authoring it — including when an entry is added and who writes the first draft.","acceptance_criteria":"A living log file with its first entries, plus a stated rule for what earns an entry and how the user co-authors it.","notes":"findings.md created with format, co-authoring rhythm and six draft entries (E1-E6) mined from the existing ledger. Stays in progress until the user confirms the format and rules on the draft entries; confirmed entries then feed the 'What we believe now' summary.","status":"closed","priority":1,"issue_type":"task","assignee":"Mike Kryjak","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T11:15:12Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T14:46:24Z","started_at":"2026-07-29T11:33:34Z","closed_at":"2026-07-29T14:46:24Z","close_reason":"findings.md created: agent-facing, terse, five findings mined from the ledger. R24 revised — the log is for Claude to load, not co-authored; the user keeps a separate record.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-219","title":"Tests for the extraction and metric code (R23)","description":"R23: hundreds of rows will be machine-produced, so a bug in extraction silently corrupts the record and every conclusion from it. Needs unit tests over the extraction and metric code, plus a re-derivation check that regenerates a stored row from raw artefacts and confirms it still matches. Depends on the results store design (so-nh3) fixing what a row contains.","acceptance_criteria":"Test suite covering extraction and metric computation, plus a re-derive-and-compare check runnable against any stored row.","notes":"Started 2026-07-30: perftest/tests/test_extract.py, 20 tests, run with the hermes3 spack python. Covers the guard-cell contract end to end (mask is false exactly on guards, no mask is a problem not a pass, residual shares and ddt reductions ignore 1e15 guard garbage), build_type read back from compile flags including the do-not-guess fallback, and _merge_columns so a new index column cannot disturb the file's own order. Mutation-checked: making _interior a no-op fails 4 of them.\n\nNot yet covered: recipe diffing, the row lifecycle (open/fill/conflict/unplanned), log parsing, physics gates, resid_regions coverage and the sparse floor.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T11:15:11Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T09:54:37Z","closed_at":"2026-07-31T09:54:37Z","close_reason":"Both halves of the acceptance criterion are met.\n\nUnit tests: 52 total, up from 20. The new 32 cover what was uncovered — log_view parsing (event names with spaces, the object table not being swallowed, three distinct error modes for missing console / no report / shifted format), options_left returning None rather than an empty list when never checked, console trimming, BOUT.log.0 parsing including the optional SNES-failures field, recipe diffing (bare petsc flags, numeric equality, canonical ordering) and the row lifecycle (declared values never overwritten, two open rows refused, float round-trip, unknown columns reported, hand-added columns surviving a write). Mutation-checked: making the numeric comparison literal, letting fill_row overwrite, and splitting event rows from the left each fail a test.\n\nRe-derive-and-compare: perftest/verify.py plus cli/verify_store.py, comparing bundle tables as well as index columns — the distinction that matters, since the guard-cell corruption lived in resid_regions.tsv and an index-only check could never have caught it. Run over the whole store: 29 checked, 29 match, 0 differ, 8 skipped because R8 already deleted their case directories.\n\nIts limit is documented in the module: it proves today's code still produces what is on disk, not that the stored value was ever right.\n\nNot covered, deliberately: physics gates (ne_sep/te_sep unfilled pending a separatrix convention, so-joa) and the resid_regions sparse floor.","dependencies":[{"issue_id":"so-219","depends_on_id":"so-nh3","type":"blocks","created_at":"2026-07-29T12:15:21Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-219","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-29T13:35:18Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-219","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T15:50:12Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-f4m","title":"Measure concurrency and slot effects, then run-to-run scatter (R20)","description":"Priority is the concurrency effect, not fine-grained precision: how much slower is a test when three run at once versus alone, and do the three 10-core slots differ from each other. Slot was previously recorded only as free text and the conduction A/B spanned slots, so past A/B comparisons are unprotected. Run-to-run scatter at fixed loading follows from the same experiment. Cheap on short tests; decide whether scratch tests get a scatter estimate at all given they cost tens of hours.","acceptance_criteria":"Concurrency penalty and slot-to-slot spread for at least one short test, plus the margin a result must clear to count as an improvement.","notes":"Unblocked 2026-07-30: it had depended on the whole diagnostics epic, which also contained it, so it could never become ready. Its real prerequisites — the capture set and the record extractor — are both closed.\n\nNeeds new runs, so it needs the user's launch gate, unlike the index, extraction and plotting work which all run on data already on disk. That is why the descriptive table and plots come first.","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T11:15:09Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T17:54:42Z","closed_at":"2026-07-30T17:54:42Z","close_reason":"Measured 2026-07-30 with six identical SNES-MUMPS-1 runs of test4steady, three alone and three at once. Slot spread alone 0.34%, so slot is not a variable; concurrency penalty +9.9% (8.2-11.8% by slot); slot spread under load 3.0%. Run-to-run scatter was already available from the 2026-07-29 triples at ~1%, so no repeats were run. Margin set: at matched loading a wall-clock difference under about 12% is unproven, at mixed loading correct by 10% or do not compare. Recorded as F22 and as six rows in the store. Scratch-test scatter deliberately not estimated - the penalty transfers and the runs cost tens of hours.","dependencies":[{"issue_id":"so-f4m","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-29T13:35:21Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-f4m","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:56Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"so-7n9","title":"Beads conventions for this project (R13): use bd to full potential, document + teach","description":"R13 changed: no longer 'keep the workflow simple'. Use beads to its full potential — dependencies, epics, structured workflows (formulas/molecules), bd remember for stored knowledge, bd doctor/preflight health checks. the user is new to beads but intends to learn it properly, so the conventions note must teach as it documents (R12 style: define terms on first use). Complexity lives in the tracker, never in the human's head (R11, top priority).","status":"closed","priority":1,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-27T08:54:53Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T14:46:07Z","closed_at":"2026-07-29T12:36:33Z","close_reason":"Conventions written into CLAUDE.md: four epics with parent-child wiring, ready-queue kept near four via dependencies not priorities, priority encodes sequence, issues titled by the requirement they serve, name-not-ID in conversation, and where each kind of content lives (requirements / findings / beads / ledger).","dependencies":[{"issue_id":"so-7n9","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-07-29T13:35:22Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-r53","title":"Diagnostics deep-dive (R9/R10): decide capture set + postprocessing package","status":"open","priority":1,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-27T08:54:51Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T13:14:57Z","dependencies":[{"issue_id":"so-r53","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-29T13:35:00Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-jgj","title":"Campaign reports: a study that reads the index","description":"Built 2026-09-20: a campaign_scan study in the store's analysis script reads every recorded row in whatever index `store` points at and builds its case selection from them, so a campaign of twenty hashed case names needs no hand-written list. Study functions are called at report time, which is what makes this possible, so no change was needed in sdtools. Verified against the four test-drive rows: all four found, labelled by what varied, table and pages correct.\n\nLabelling: the window joins the label only when the selection spans more than one window. The first version numbered them instead, which rendered two different windows as \"baseline\" and \"baseline #2\" — reading as repeats of each other when their counts are not comparable at all. Caught by looking at the rendered page.\n\nKnown and NOT fixed, because it is outside what was asked for: with a selection spanning several windows, scan_summary's \"vs prev\" and \"vs base\" columns compare across windows and are meaningless there. A single-window campaign, which is the intended shape, does not hit it.","status":"closed","priority":2,"issue_type":"feature","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T20:17:32Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T20:17:39Z","closed_at":"2026-09-20T20:17:39Z","close_reason":"Built and verified on the four test-drive rows; the PDF renders with every case found and correctly labelled.","dependencies":[{"issue_id":"so-jgj","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T21:17:38Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-cv2","title":"schema.md still describes the extractor as it was before the six outcomes","description":"The store's schema.md is now wrong in four places, all caused by the extractor work of 2026-09-20. It is documentation in the store repo, so changing it needs the authorisation phrase.\n\n1. nl_its, lin_its and solver_fails each say the cells are empty on a CVODE run and that nothing copies the counters into them. The extractor now fills all three from the dump's cumulative counters, last value rather than a sum, for a run with no SNES log.\n2. The outcome row lists the five older classes: diverged_at_start, snes_failure, linear_failure, crawl_aborted, completed_wrong. The extractor now decides six: completed, invalid, diverged, timeout, crashed, cancelled. The older names, and stalled, should stay listed as historical values that predate them, because rows in the index still carry them.\n3. The 'still open' item saying the extractor does not decide every outcome can be closed.\n4. ms_per_24h should say it is written only for a completed run.\n\nThe same reconciliation is owed by requirements.md R19, which still names the old five classes, and design.md section 7, which names the six but not the rules that decide them.","acceptance_criteria":"schema.md's nl_its, lin_its and solver_fails rows say the values come from the log for a SNES run and from the dump's cumulative counters, last value not summed, for a CVODE run. The outcome row lists the six current classes and marks the five older names plus stalled as historical. The 'still open' item about the extractor not deciding every outcome is gone. ms_per_24h says it is written only for a completed run. R19 and design.md section 7 name the same six.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T17:32:27Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T18:05:26Z","dependencies":[{"issue_id":"so-cv2","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T18:32:34Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-yrg","title":"Report cover is agent notes, not something to show anyone","description":"The generated PDF is 13 pages and the first six are the cover: a wall of small monospace text holding the analysis environment, a notes block, the cases table and the differing-options table. It is useful to an agent checking what went into a report. It is the wrong thing to put in front of a person, and it is what they see first.\n\nLooked at directly on 2026-09-20 by rendering the pages. The plot pages are good - clear labels, readable at a glance, one idea each. The cover is the weak part, and it is six times longer than any plot page.\n\nWhat is wanted: a presentable mode. One cover page carrying the title, the date, the configuration and the case list, with the diagnostic detail either dropped or moved to the back. The current cover stays available for internal use.\n\nThe cover is built by sdtools' pages/cover.py, shared with the user's other campaigns, so a change there affects them too. Check before editing.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T16:44:58Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T16:44:58Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-4xt","title":"CVODE runs record no iteration counts","description":"Both CVODE-2 rows have blank nl_its, lin_its and solver_fails, so no cost comparison across solver families is possible today.\n\nCause, found while fixing the schema: extract.py sets nl_its and lin_its only inside the 'if not snes.empty' branch, from the SNES log dataframe. The CVODE counters cvode_nniters and cvode_nliters are read into the bundle series but nothing copies them into the index columns.\n\nFix: in the dump branch, when the SNES dataframe is empty, take the LAST value of cvode_nniters and cvode_nliters and write them to nl_its and lin_its. CVODE's counters are cumulative over the run, so the last value is the total. Do not sum them the way the SNES per-step columns are summed. cvode_nonlin_fails fills solver_fails the same way.\n\nThe schema no longer promises this, so documentation and code now agree that it does not happen. This issue is to close the gap the other way.","status":"closed","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:56:28Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:32:44Z","closed_at":"2026-09-20T17:32:44Z","close_reason":"Delivered 2026-09-20 and committed. Extractor: six outcomes decided from the log and the dump, divergence tested before the clock, an interrupt never read as a crash, ms_per_24h written only for a completed run; CVODE counts now come from the dump's cumulative counters, last value not summed, verified against both CVODE rows. Campaign config and runner: campaign.toml with an enforced approval record, the section 8 loop, resumable from the index, cutoff set only by completed runs. Reports: an outcome page before any timing, DNF for a blank wall clock, the convergence-test bar shown as each run's own share with the count kept as a label, PDFs written to the data directory. 136 tests pass. Two gaps filed separately: the runner's kill outcome never reaching the index, and the launch call this workstation blocks.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-bq1","title":"Console entry points swallow the friendly error messages","description":"query.py and can_delete.py each wrap main() in a try/except under the __main__ guard and print a one-line message for their own exception type: 'query.py: \u003cproblem\u003e' and 'can_delete.py: REFUSED: \u003creason\u003e'.\n\nA console entry point calls main() directly, so that block never runs. Under the installed names query-store and can-delete, those two errors surface as a traceback instead.\n\nThis matters most for can-delete, whose whole job is to refuse clearly. The run procedure calls can_delete.py by script name via PATH, so the guard works as used today; the installed alias is the broken path.\n\nFix: move each handler inside main(). Small, and it pairs with removing the now-redundant sys.path.insert lines from all five cli tools, which is the other edit those files want.","status":"closed","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:56:19Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:18:55Z","closed_at":"2026-09-20T17:18:55Z","close_reason":"Handlers moved inside main() in query.py and can_delete.py; both the script path and a direct main() call now print the friendly message and return the exit code (query 2, can_delete the refusal status). The other half of this issue is refused: the sys.path.insert lines are NOT redundant. hermes-solver-opt is installed in no interpreter (neither pyenv nor the hermes3 spack view can import solveropt_cli), and .bashrc line 171 puts cli/ on PATH, so the tools run by name and the path line is what makes perftest importable. Installing into the spack view is forbidden by standing rule, so the lines stay permanently, not just for now.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-4b8","title":"Measure what share of the residual norm the scaling floor carries","description":"The scale_vars floor is reached in nearly every run - fourteen of sixteen runs report a minimum scaling factor of exactly rtol. That proves the floor activates. It does not prove the floored elements matter.\n\nA handful of elements out of 5600 could contribute nothing to the residual norm, or could dominate it. Which one is true decides whether any residual-based objective is usable before the code defect is fixed, and that objective is on the roadmap for pseudo-transient methods.\n\nThis needs no new runs. The residual data is already stored: resid_final, resid_drop and resid_per_rhs in the index, and resid_regions.tsv in every bundle.\n\nWhat to produce: for one or two runs, the fraction of the squared residual norm contributed by elements whose scaling factor sits at the floor, and which variables those elements belong to. The expectation is that they are momenta near stagnation points, since densities and temperatures are floored positive by the physics and stay clear of rtol.\n\nFollow the postprocess skill. If the stored per-region data cannot separate floored elements, say so and state what capture would be needed, rather than approximating.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:45:17Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:45:17Z","dependencies":[{"issue_id":"so-4b8","depends_on_id":"so-bt1","type":"parent-child","created_at":"2026-09-20T16:45:25Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-aa6","title":"Schema drift found by the query tool","description":"Six things the new query tool surfaced while reading index.tsv. None blocks it, all mislead a reader.\n\n1. schema.md says 'about forty' columns and claims INDEX_COLUMNS holds 51. The file has 56. The doc's count is stale.\n2. One row carries outcome=stalled, which is in no enum in schema.md.\n3. schema.md documents no 'project' column, although it is column 2 and perf-bisect rows depend on it.\n4. Both CVODE-2 rows have blank nl_its and lin_its, although schema.md says CVODE counts come from its dump counters. Any cost comparison across solver families is missing the CVODE iteration counts.\n5. verdict reads no_reference on 47 of 48 rows and blank on one, so it carries no information yet. Expected until the reference rule is decided, but worth stating in the schema so nobody reads it as a result.\n6. 'varied' is not alphabetically sorted in practice, although schema.md makes that a rule. Grouping by varied will therefore split runs that tested the same combination.\n\nAlso worth knowing for anyone reading group counts: six of the seven cancelled rows carry nl_its, lin_its and solver_fails but blank wall_s, so a group's n can be much larger than the sample behind a given median.","status":"closed","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:35:25Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T16:53:38Z","closed_at":"2026-09-20T16:53:38Z","close_reason":"Fixed in solver-opt-store 300c5eb. All six corrected in schema.md: the column count is gone rather than wrong, project is documented, stalled is recorded as predating the settled classes, the CVODE promise is withdrawn to match the code, verdict says it carries no information yet, and the varied sorting rule keeps a caveat that nothing enforces it. A seventh was found and fixed at the same time: solver_fails carried the same false CVODE promise.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-w2d","title":"Mine the captured PETSc view for the knob dictionary (R9)","description":"The SNES and KSP objects PETSc actually built for SNES-MUMPS-3 are captured in the store at runs/test2_20.0-21.0ms-20260920-160050/petsc_views.txt. That file lists every knob in force, defaults included, which is a better starting point for the search space than PETSc's documentation - it describes this build and this version.\n\nOne default already looks worth a look: fgmres restart=30 against maxl=260, so nearly nine GMRES restarts inside one solve, each discarding convergence history. Nobody chose 30.\n\nCapturing a view for another recipe is one short window plus cli/add_views.py, run by hand. It is deliberately not part of the run procedure: a view is identical for every run of one recipe, and PETSc prints it once per solve, so a long run writes gigabytes of it.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:06:12Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:32Z","closed_at":"2026-09-20T15:35:32Z","close_reason":"The knob dictionary is written: search-space.toml, 62 knobs in six groups, committed in 2915563. Every solver: name verified programmatically against the BOUT++ SNES source; PETSc-side defaults taken from the captured view rather than the manual. The view mining is folded in - the GMRES restart mismatch, the unread colouring flag and the scale_vars defect are all recorded as entries.","dependencies":[{"issue_id":"so-w2d","depends_on_id":"so-drc","type":"parent-child","created_at":"2026-09-20T16:06:20Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-v37","title":"A human-readable presentation of the project's rules (R11)","description":"requirements.md is Claude's log of discussions with the user. The user never wrote or read it, and it is too verbose to read. On 2026-09-19 the user asked for a separate presentation that makes the rules human-readable. Open: its form (a page, a doc or a short file), and how it stays in sync with requirements.md without becoming a second source of truth. Pending rulings, such as the old July proposals on noise repeats and on what counts as this project's run, should reach the user through it, restated plainly.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-19T15:22:05Z","created_by":"Mike Kryjak","updated_at":"2026-09-19T15:22:05Z","dependencies":[{"issue_id":"so-v37","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-09-19T16:22:14Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-ald","title":"Full-run estimate check: summed window times against a real full run (R39)","description":"Run 10 to 15 configurations at both a rung 0 window and the full run and compute the rank correlation between the orderings. Below 0.8 the ladder is worse than no ladder. Roughly 15 test4 full runs, about five hours across three slots. Run when there is time; recompute as runs accumulate.","notes":"Retitled 2026-09-19. The user rejected trusting a ladder by rank correlation, because the best settings may be slow early and fast late. R39 replaces it: estimate the full-run time by summing each stretch of the run at its window's measured speed, and check that estimate against a real full run from time to time. This issue is now that check.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:45Z","created_by":"Mike Kryjak","updated_at":"2026-09-19T16:26:08Z","dependencies":[{"issue_id":"so-ald","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:03Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-ald","depends_on_id":"so-dbq","type":"blocks","created_at":"2026-09-18T13:42:08Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-37r","title":"Two-level correctness check","description":"Window check on every rung: a sanity band against the parent on the four quantities. Endpoint check on the full run only: same steady state as the reference. Pass/fail flag beside the cost, never inside it. Blocked on the separatrix definition and the tolerance values, which go in campaign.toml.","notes":"Set by the user 2026-09-19: for test 2, the parent run test2_0.0-100.0ms-20260919-173706 is the reference steady state (target Te max still rising ~1% per 10 ms at 100 ms, judged close enough). A full run passes if its endpoint is within 5% of that run's endpoint. Which quantities the 5% applies to is not yet fixed; the extracted physics set is ne_omp_sep, te_omp_sep, ne_target_max, te_target_max.\n2026-09-19 later: the zero-neutral test 2 parent (test2_0.0-100.0ms-20260919-173706) was deleted at the user's request: case, store row, bundle and seeds. The rerun with non-zero initial neutrals takes the case name test2_0.0-100.0ms-2026-09-19-neutlag-va and becomes the test 2 parent and reference. Test 4 and test 5 parents queued overnight with the same settings.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:42Z","created_by":"Mike Kryjak","updated_at":"2026-09-19T19:13:37Z","dependencies":[{"issue_id":"so-37r","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:00Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-37r","depends_on_id":"so-dbq","type":"blocks","created_at":"2026-09-18T13:42:09Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-zq3","title":"Content-addressed run identity","description":"Hash the resolved configuration - normalised recipe with overrides, test, commit, build flags, cores, pinning - into the run identity, so an accidental duplicate and a deliberate repeat are mechanically different. Record the campaign config hash in every row too.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:41Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:41:41Z","dependencies":[{"issue_id":"so-zq3","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:41:58Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-erp","title":"Line-search tuning is the next lever on test5, not the Jacobian","description":"Where the CVODE-like scan leaves test5steady, and what to attack next.\n\nBest configuration is target_its=5 plus snes_linesearch_type=bt on SNES-MUMPS-2: 208 s against a 676 s baseline and 1249 s for CVODE-2. Physics unchanged to 0.002%.\n\nJacobian construction is still 80% of that run, but it CANNOT be reduced by lagging. Measured sweep on the best configuration, assembled operator:\n\n  lag   wall    ncalls   nl_its  fails   jac   t_jac\n   3    208 s   133986    1204     96    803   0.805\n   5    212 s   135812    1249    154    789   0.779\n  10    241 s   154348    1154    243    866   0.756\n\nRaising the lag does not reduce builds. It raises FAILURES - 96 to 154 to 243 - because a staler Jacobian gives worse Newton steps, and every failure calls SNESSetLagJacobian(snes, 1) (snes.cxx:857), restored only after a success, so the whole cascade rebuilds per iteration. The scheduled saving and the failure-driven cost cancel, and past lag 5 the failures win.\n\nGeneral principle worth carrying to other tests: lag_jacobian pays only where the solve is nearly failure-free. F18 got -58% from lag 1 to 3 on test4steady with an identical trajectory - and test4steady at that point had one failure. Where failures are common the lever is self-defeating.\n\nSo builds are failure-dominated, and the remaining 96 failures are the target. Their character has changed since the scan began: at the baseline they were 56% DTOL divergence and 44% max-iterations; at the best configuration they are 79% line-search failures (-6) and 21% max-iterations. The solver is no longer blowing up - it is failing to find an acceptable step length.\n\nThat makes line-search tuning the next scan, and it is completely untouched: the alternative types (l2, cp, basic with damping), the damping parameter, maxstep, and whether the line search should be allowed more function evaluations. Each is a one-line change on a 208 s test.","acceptance_criteria":"A line-search scan on test5steady from the 208 s configuration, reporting failure count by reason and wall clock per variant.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T14:09:48Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:30Z","closed_at":"2026-09-18T12:42:30Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-erp","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T15:09:53Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-xi1","title":"test5 count scatter looks ~10x worse than test4's, and the scan depends on it","description":"The CVODE-like scan's baseline repeat came out 11% above the 2026-07-30 row on work counts, with only diagnose_failures intentionally different between them:\n\n                          wall   ncalls   nl_its  fails  jac builds  concurrency\n  2026-07-30 va-mumps2    638 s  385102     913    661     2406         2.09\n  2026-08-01 baseline     676 s  438333    1007    731     2739         1.00\n\nPhysics is identical - peak target Te agrees to 0.003% - so this is the same answer reached by 11% more work.\n\nTwo explanations and one run cannot separate them:\n- diagnose_failures perturbs the trajectory. It should not: it only takes min/max reductions over fields on failure. But it does add roughly 10k collective reductions, and F16's mechanism for non-determinism is precisely that reductions are not bitwise reproducible and convergence tests near threshold then tip differently.\n- test5's count scatter is genuinely much larger than test4's. F16 measured 1.8% on test4steady. test5 has 731 failures, each one a decision near a threshold, so the failure count is itself the thing scattering and it amplifies.\n\nEither way this matters immediately: F16's 1.8% figure is a test4 number and must not be used to judge test5 differences. If the true scatter here is ~10%, then any step in the scan worth under about 15% is unproven from one run each.\n\nThe scan itself is unaffected - every step is compared against this baseline under identical conditions, which is why the repeat was included.\n\nCheapest resolution: one more repeat of the baseline configuration. If it lands near 438k the option is innocent and the scatter is real; if it lands near 385k, diagnose_failures is the cause.","acceptance_criteria":"A second baseline repeat, and a stated count-scatter figure for test5steady that the scan's conclusions are read against.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T11:58:51Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:31Z","closed_at":"2026-09-18T12:42:31Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-xi1","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T12:58:57Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-xwt","title":"Cap kappa_par_e as a diagnostic: is the dt ceiling conduction-set?","description":"Establishes whether the flash is what sets the maximum stable step on test5, as opposed to being a passenger.\n\nTwo knobs, both available now:\n- e:conduction_kappa_max, already implemented in Hermes-3 from the cf project. Harmonic cap applied after the physical limiter: kappa_eff = kappa * kappa_max / (kappa + kappa_max). Smooth, so it does not introduce a residual kink.\n- e:kappa_limit_alpha = 0.2, matching both test4s. test5 currently runs with the limiter OFF (-1), so kappa_par is uncapped Spitzer at 119-288 eV and around 1e18 m-3.\n\nBoth CHANGE THE PHYSICS. They are diagnostics for the ceiling question, not entries in the speed table, and any result must be reported that way.\n\nPrediction if the conduction reading is right: the failure count collapses and the accepted timestep rises by roughly the factor kappa is reduced by. If the failures survive an aggressive cap, the ceiling is set by something else and this whole epic needs rethinking.\n\nWorth knowing independently of the solver: an unlimited Spitzer parallel heat flux at these conditions may be exceeding the free-streaming limit, which would be a physics problem in the test case itself.","acceptance_criteria":"test5steady at two or more kappa caps, reporting failure count, median and peak accepted timestep, and the physics drift against the uncapped baseline.","notes":"Intake note, 2026-08-01: conduction_kappa_max is cf instrumentation, built for the cf question and their prepared st40 cases. This issue is the first instance of the standing arrangement - candidate fixes originate under cf-nyl, and get tested for performance here. Report the result back to that side rather than concluding about the flash from a failure count.\nCorrection 2026-08-03: e:conduction_kappa_max is no longer in current hermes-3 source - the May implementation survives only on the local explore-fix-flashes branch (commit 32444608). The cf side is rebasing it onto latest master as a local-only branch under its new test5 fix campaign (epic cf-nyl.8), which also fixes the campaign's ground rules: CVODE, nout \u003c= 200, dumps retained. The kappa-cap runs this issue wants are now cf-nyl.8.4; coordinate there rather than preparing an independent capped run.\nCost verdict from the cf side, 2026-08-04 (their report output/01_test5_flash_deep_dive.pdf, pages 22-24): the dt ceiling is NOT conduction-flash-set on the test5 CVODE cases. Evidence: (1) the flashes were proven to be integrator-noise readouts with zero trajectory imprint (amplitude falls 267-288x for 100x tighter rtol); (2) capping kappa by up to 150x leaves total CVODE work within 2% of baseline; (3) on test5stiff at 4x output cadence, work-per-output vs flash amplitude gives raw Spearman +0.51 but -0.17 after first-differencing - the historical 0.89 was a shared-drift artifact of coarse raw series. Treat amplitude-vs-cost claims from raw store series as unproven; detrend first. The prediction in this issue (failure count collapses / dt rises with kappa reduction) is answered negative for CVODE; whether it holds for SNES failures remains this side's question.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:13:01Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:31Z","closed_at":"2026-09-18T12:42:31Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-xwt","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T09:13:10Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-zk3","title":"Line search: basic to bt on the MUMPS recipes","description":"Both MUMPS recipes set petsc:snes_linesearch_type = basic - a full undamped Newton step with no backtracking. When the controller demands 2.9x and the step is too long, nothing inside the solve can recover; it can only fail and be rejected.\n\nPETSc default bt would cut the step back and converge instead. This is the part that converts a controller overshoot into a hard failure, and it is one line.\n\nExpect it to interact with so-yjs.2 and with the tolerance issue: all three reduce failures by different routes. Order matters for attribution, so run it on its own.\n\nNote the flash reading: if the failures are localised over-long steps into the flash cells, a line search is exactly the right remedy and does not change the answer. diagnose_failures would say so directly.","acceptance_criteria":"test5steady with snes_linesearch_type = bt, failure count and wall against the SNES-MUMPS-2 row, physics unchanged.","notes":"MEASURED 2026-08-01 on test5steady, as step 2 of the CVODE-like scan, on top of target_its = 5. snes_linesearch_type basic -\u003e bt, everything else held:\n\n                     wall    ncalls   nl_its  fails  jac builds\n  + target_its 5     432 s   279790    1210    311     1748\n  + linesearch bt    208 s   133986    1204     96      803\n                     -52%     -52%      -0%   -69%     -54%\n\nPhysics unchanged: peak target Te 131.0486 against the baseline's 131.0465, 0.0016%.\n\nThe failure profile confirms the mechanism exactly, and this is the cleanest causal evidence in the project so far:\n\n  reason              baseline   +target_its   +bt\n  -9  DTOL              412         151          0\n  -5  MAX_IT            319         160         21\n  -6  LINE_SEARCH         0           0         75\n\nEvery DTOL divergence is gone. Those were undamped full Newton steps blowing the residual up by four orders of magnitude; with a line search the same events now appear as 75 line-search failures, meaning the solver looked for a shorter step and reported when it could not find one, instead of taking the long one and diverging.\n\nNonlinear iterations are flat at ~1204 while failures fell 69% and Jacobian builds 54%. Same Newton work, far less of it wasted.\n\nCumulative with step 1: 676 s -\u003e 208 s, 3.25x, from two one-line settings. SNES is now 6.0x faster than CVODE-2 on this test in wall clock and 6.1x in RHS evaluations - the test where SNES was believed to lose.\n\nStill to attack: t_jac_frac is 0.805, so building the Jacobian is still 80% of the run and the matrix-free step is where the remaining headroom is.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:13:00Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:32Z","closed_at":"2026-09-18T12:42:32Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-zk3","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T09:13:10Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-hgc","title":"Tighten atol on test5, separately from the controller","description":"atol = 1e-3 on the MUMPS-2 recipe is doing three things at once on test5, and all three hurt:\n\n1. It lets the Te corrugations grow. Measured controlled on test4steady across the tolerance step of the MUMPS-1 to MUMPS-2 scan (atol 1e-7 to 1e-3, rtol 1e-6 to 1e-5, same case, same build): max|ddt(Pe)| median 2.57e6 to 9.11e7 (35x), rms 2.09e5 to 3.37e6 (16x), max/rms 12.5 to 22.8. Peak dt is nearly identical between the two runs (4.54e4 against 4.87e4) so the step-interpolation artefact cannot account for it.\n2. It hollows out each solve. 354 of 470 accepted steps on test5steady converge in ONE Newton iteration.\n3. Through (2) it pins the timestep controller at its maximum. facP = (100/nl_its)^0.233, so nl_its=1 gives 2.92, and the controller demands that growth after three quarters of the steps.\n\nThe same tolerance loosening was measured as a 46 percent saving on test4steady (F18), on a case with the flux limiter on and 1300x less conduction stiffness. Carrying it to test5 imports all three effects into the one case that cannot absorb them.\n\nRun this SEPARATELY from the target_its change (so-yjs.2). Both calm the controller; only the tolerance touches the fluctuations. Running them together confounds the controller-overshoot term with the fluctuation-driven Newton-radius term, and telling those apart is the whole question.","acceptance_criteria":"test5steady at tightened atol, reported against so-yjs.2 run at loose atol, with failure count, Jacobian builds, nl_its distribution and max|ddt(Pe)| for each.","notes":"MEASURED 2026-08-01 on test5steady as step 3 of the CVODE-like scan, and it is a decisive NEGATIVE. atol 1e-3 -\u003e 1e-7, rtol 1e-5 -\u003e 1e-6, on top of target_its=5 and linesearch bt:\n\n                     wall     ncalls   nl_its  fails  jac builds  accepted steps\n  + linesearch bt     208 s   133986    1204     96      803        ~700\n  + tight tolerance  2071 s  1359841   10408    954     8148        3695\n                     +896%    +915%    +764%\n\nTen times slower. Whatever fluctuation benefit F28 predicts is swamped by an order of magnitude of extra solver work, so tightening tolerance is wrong on test5 on cost grounds. Do not carry this into a recipe.\n\nThe mechanism is the coupling this issue was warned about, and it runs the unhelpful way. Tightening lifts nl_its off 1 - the distribution moves from 'almost always 1' to a spread with mode 2 and a long tail - and the controller reads nl_its: facP = (target_its/nl_its)^0.233. At target_its=5, nl_its=1 grows dt by 1.44 per success; nl_its=2 grows it by 1.23 and nl_its=3 by 1.13. So tightening the tolerance SLOWS dt growth, dt stays small, and the run needs 3695 accepted steps instead of about 700 - while each of those steps also costs several times more Newton iterations.\n\nDesign consequence, and it generalises beyond this run: target_its cannot be chosen independently of the tolerance, because the tolerance sets the nl_its the controller reads. target_its should sit just above the typical nl_its that the tolerance produces, which is the only way the controller becomes genuinely two-way. At this tolerance the right target would be about 2-3, not 5. Nobody has tested that pairing.\n\nThis also poisons step 4 of the ladder, which inherits the tolerance. A branch run (test5steady-2026-08-01-va-step3b-matrixfree) puts matrix-free on step 2 instead.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:12:59Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:30Z","closed_at":"2026-09-18T12:42:30Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-hgc","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T09:13:09Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-935","title":"Add a non-interactive flag to sdrun.py and reset_test.py","description":"Both prompt with input() and offer no way to skip: sdrun.py at cli/sdrun.py:302 ('Proceed? [y/N]') and reset_test.py at cli/reset_test.py:88 ('Delete these files and reset the case?'). Any script driving a sequence of runs blocks on the first one. extract_test.py does not prompt, so it is already fine.\n\nMirror what hcompile.sh gained on 2026-07-31: a -y flag that skips the prompt, required when stdin is not a terminal, so an accidental non-interactive call aborts instead of running unattended. Same flag name and same behaviour in all three, so there is one thing to remember.\n\nBlocks the perf-bisect driver, which cannot run twelve anchors unattended until this lands.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T11:31:14Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T17:33:39Z","closed_at":"2026-07-31T17:33:39Z","close_reason":"Already satisfied: sdrun.py and reset_test.py both take -y. run_ladder.py drives both non-interactively.","dependencies":[{"issue_id":"so-935","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-07-31T12:31:29Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-57b","title":"Make hcompile.sh parameterised enough to drive from a script","description":"perf-bisect cannot drive builds until this is fixed, and the fix is worth having regardless. Read of /home/mike/work/hcompile.sh (170 lines) as of 2026-07-31:\n\nBlockers for automation\n- BUILD_DIR, LIMITER, CONDMETHOD and HERMES_DIR are hardcoded top-of-file variables edited by hand, above a graveyard of ~15 commented-out alternatives. These are exactly the four things perf-bisect must vary per invocation, and none has a flag.\n- PETSC_DIR is hardcoded to /home/mike/work/petsc-3.23.3. That pins PETSc for every build and is the variable the feasibility probe may need to move.\n- cmake --build -j 32 is hardcoded. A 32-way build overlapping a timed run corrupts the timing; the job count must be settable. perf-bisect is serial so it cannot self-collide, but it can collide with a solver-opt run in another slot.\n- No set -e. A failed cmake configure falls through to cmake --build, so the script can report the build's status for a configure that never succeeded. Automation needs an unambiguous pass/fail.\n- cd $HERMES_DIR is relative, so the script only works when run from /home/mike/work.\n- Nothing records what was built - no commit sha captured anywhere.\n\nBug found while reading, independent of perf-bisect\n- The -e guard rejects any configure-affecting option and tests CHECK_LEVEL != 0, but CHECK_LEVEL defaults to 2 and -d sets it to 4. So -e can never pass its own guard: express mode always exits 2. The guard should compare against the defaults it actually has (2 and RelWithDebInfo), not 0.\n\nRecommended scope: do NOT rewrite. Turn the four hardcoded variables into flags whose defaults are today's values, add set -e, and make -j settable. Every existing invocation then behaves byte-identically. The script is shared with the physics-build campaigns and a redesign risks silently changing builds those campaigns depend on.\n\nOpen question: whether the -e guard bug should be fixed in the same change or separately, since fixing it makes a currently-dead code path live.","notes":"Rewritten 2026-07-31, tested with a stub cmake against a throwaway repo; awaiting the user's review of the diff before closing. Original kept at /home/mike/work/hcompile.sh.orig (that directory is not a git repo, so there was no other undo).\n\nDone:\n- BUILD_NAME (-b), LIMITER (-s), CONDMETHOD (-m), HERMES_DIR (-r), PETSC_DIR (-P), jobs (-j), CHECK (-k) are now flags. Defaults reproduce the previous hardcoded values exactly, so a bare invocation builds what it built before.\n- Build directory is composed, not given: build-\u003cva|mc\u003e-\u003cname\u003e. Two builds differing only by slope limiter can no longer overwrite each other.\n- set -e and set -o pipefail, so a failed configure no longer falls through to the build step.\n- Relative -r resolves against the script's own directory, so it runs from any cwd.\n- Settings validated up front (limiter, conduction method, numeric CHECK and jobs, repo exists) rather than failing inside CMake.\n- Prints the commit sha and branch it is building, plus the full configuration.\n- The commented-out BUILD_DIR graveyard is gone; those names survive in the .orig backup.\n\nThe -e guard bug was fixed in the same change, contrary to the open question, because making CHECK a flag removed the option of leaving it: the old guard inferred 'user passed a configure option' by comparing CHECK_LEVEL against 0, which cannot work once any value is settable. It now tracks which configure-affecting flags were actually parsed. Express mode works for the first time; -b, -s, -r and -j are permitted with it since they only locate the build or control the build step.\n\nNot done, deliberately: only the slope limiter is baked into the name, as asked. Conduction method is not, so build-va-master built with Harmonic still silently overwrites build-va-master built with Original.\n\nFollow-up 2026-07-31, all tested with a stub cmake and a real pty:\n- Confirmation summary in sdrun.py's style (orange box rule, dim labels, yellow git info, green flags), showing repo, commit, physics options, build type, PETSc dir, jobs, mode and extra flags, then Proceed? [y/N].\n- -y skips the prompt. Required when stdout/stdin is not a terminal: without it a non-interactive call aborts rather than building unattended. This is what lets perf-bisect drive the script. Stricter than sdrun.py, which accepts a piped answer.\n- Conduction method now appended to the build name when it is not Original: build-va-harmonic-master, build-mc-productjk-master. Original stays out, so existing names are unchanged.\n- Default job count changed from 32 to 1, at the user's request. Protects timed runs from a build stealing cores; the cost is that an interactive full build now needs an explicit -j.\n- The -l build log is written to the directory the script was called from, not the repo it builds. It was already doing this by accident (the redirect preceded the cd); now it is explicit, and the redirect happens after the prompt so the summary stays on screen.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:56:03Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T17:33:37Z","closed_at":"2026-07-31T17:33:37Z","close_reason":"Already satisfied: hcompile.sh takes -b name, -s limiter, -m method, -r repo, -j jobs, -S slot and -y non-interactive, which is everything run_ladder.py needs to drive it.","dependencies":[{"issue_id":"so-57b","depends_on_id":"so-tg6","type":"parent-child","created_at":"2026-07-31T11:56:09Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-psq","title":"perf-bisect: a commits lookup table joining sha to date and subject","description":"index.tsv records hermes_commit as a bare sha and carries no commit date at all, so no row in the store can be read as old or new code without looking the sha up by hand. perf-bisect makes this acute (the commit is its independent variable) but the gap is not specific to it.\n\nRecommendation: one commits table, not a second run ledger. Two files of one-row-per-run is how the two projects get confused; one run ledger plus one lookup table cannot be, because they are different shapes and join on the sha. Proposed commits.tsv in perftest-results, keyed by hermes_commit: date, subject, bout_commit, and for perf-bisect rows the anchor flag and why that commit was chosen. Every solver-opt row gets human-readable provenance for free; no date or subject is duplicated into index.tsv, where it would drift and add noise (R11).\n\nOpen question: whether extraction should append a row automatically for any commit it has not seen, or whether the table is curated by hand for anchors only.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:51:18Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:56:08Z","closed_at":"2026-09-20T15:56:08Z","close_reason":"commits.tsv and make_commits_table.py are in notebooks/perftests/perf-bisect/, committed in 481426b. Twelve anchors joined to date, subject and the BOUT++ commit each pins. The generator rebuilds from ladder.toml and the index, so a new run's commit appears without hand editing, which also settles the issue's open question about whether extraction should append rows.","dependencies":[{"issue_id":"so-psq","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:51:23Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-372","title":"perf-bisect: build-run-discard automation under a hard disk budget","description":"Disk decides the loop shape: 69 GB free of 915 G (93% used) and a build dir is 3.6 GB, so 18 resident builds (65 GB) is impossible. Serial only: build one commit, run the test, extract to the store, delete the build.\n\nSettled 2026-07-31: keep NO build directories. Not the binary either. The store is the surviving record, same bargain as R8; a commit that needs re-measuring is rebuilt from its sha, which is deterministic. Peak disk is then one build, not twelve.\n\nUse one clone with a git worktree per commit rather than N clones: a worktree cannot be confused about which commit it is on, and costs a fraction of the disk.\n\nCompile licence, granted by the user 2026-07-31: the standing rule is that compiling Hermes builds is the user's job, and this is the one exception. It is made safe by location - everything clone, worktree and build lives under /home/mike/work/hermes-bisect, used exclusively for perf-bisect. Nothing here ever touches /home/mike/work/hermes-3 or its 85 GB of existing builds, and the exception does not extend to any other project.","design":"Driver shape, proposed 2026-07-31.\n\nOrchestrate, never reimplement. The driver calls hcompile.sh, reset_test.py, apply_recipe.py, sdrun.py and extract_test.py as subprocesses. Every step therefore stays runnable by hand with the same command, and there is one source of truth per operation. A driver that reimplements any of them creates a second way to build or run that drifts from the first.\n\nState lives in index.tsv, not a private state file. The driver decides what is left to do by reading the store, which is already the rule that the index defines what exists. That makes it idempotent and resumable: interrupt it, rerun it, and it picks up the anchors without rows. A private progress file would be a second record able to disagree with the index, which is the failure this project keeps designing against.\n\nPer anchor, in order: create the worktree and update submodules recursively; build pinned to the slot; reset the test; apply the recipe and the migrations for that commit; open the index row; launch; wait; extract; delete the build, the worktree and the case directory. Each step skippable if its output already exists.\n\nA build failure is data, not an abort. Old anchors failing to compile is the answer to the window question, so the driver records the outcome on the row and moves to the next anchor. It must never silently skip: an anchor with no row and no recorded failure is indistinguishable from one never attempted.\n\nEverything that defines the study - the twelve anchors, the migration ledger, nout, the recipe, the slot - is read from one config file, not embedded in the code. Slot is a parameter defaulting to 1 (cores 2-11); the user confirmed slots 2 and 3 are busy now but that will change.\n\nLives in the perf-bisect analysis directory, not sdtools: it encodes this study's choices. The reusable pieces it calls are already in sdtools and stay there.","notes":"2026-09-20 audit: reset from in_progress to open. Claimed 2026-07-31 and nothing was built; the design above is complete but no driver exists. It was showing as the only active work in the tracker, which was misleading.","status":"open","priority":2,"issue_type":"task","assignee":"Mike Kryjak","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:45:01Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:17:51Z","started_at":"2026-07-31T17:33:41Z","dependencies":[{"issue_id":"so-372","depends_on_id":"so-57b","type":"blocks","created_at":"2026-07-31T11:56:10Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-372","depends_on_id":"so-935","type":"blocks","created_at":"2026-07-31T12:31:30Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-372","depends_on_id":"so-9e6","type":"blocks","created_at":"2026-07-31T11:45:22Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-372","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:16Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-372","depends_on_id":"so-nnt","type":"blocks","created_at":"2026-07-31T11:45:21Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":4,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-8h8","title":"perf-bisect: option migration ledger for input files that changed over 12 months","description":"One input file per commit drifts and hides what actually differs. Proposal: one canonical input per test plus an ordered ledger of migrations (applies-before commit/date, section, key-then, key-now, kind, note); the input for a commit is the canonical file with every later migration applied in reverse. One rename is then one row, not N file edits. Critical distinction the ledger must encode: a rename with identical behaviour (SNES-MUMPS-1 still says pid_controller while test2scratch/BOUT.inp says timestep_control = pid_nonlinear_its) gets migrated silently; a changed default or changed semantics must NOT be migrated, because that change is part of what is being measured and forcing the old value measures a configuration nobody ran. Every materialised input goes into the run bundle. Open question: where the ledger lives, given hermes-perftest is read-only.","design":"First ledger content, determined 2026-07-31.\n\nKeying rule, and the reason the ledger cannot be a list of dates: an option's availability follows the BOUT++ commit each anchor pins, not the calendar. timestep_control replaced pid_controller in BOUT-dev on 2026-02-11 (b2102d05), but hermes-3 only picked it up at the 2026-05-15 submodule bump - three months later. A ledger keyed on anchor date would migrate three anchors wrongly. Key every row on the pinned submodule commit.\n\nMeasured across the twelve anchors:\n\n  anchor    date        BOUT-dev    option\n  a35e2f5e  2025-08-27  c4c149a28   pid_controller\n  6c73a474  2025-09-17  c4c149a28   pid_controller\n  a9f8f30d  2025-10-29  c4c149a28   pid_controller\n  e027256d  2025-11-21  aae67a54a   pid_controller\n  e0f1ebfb  2025-12-15  fc15ca8c1   pid_controller\n  f6ae2d1e  2026-01-29  7d28d67c3   pid_controller\n  920ba829  2026-03-01  7d28d67c3   pid_controller\n  e1fe3af7  2026-03-26  7d28d67c3   pid_controller\n  8220ead4  2026-04-24  7d28d67c3   pid_controller\n  2eeda48f  2026-05-15  8218f71a2   timestep_control\n  1084ad65  2026-06-25  c7020b4fb   timestep_control\n  20581461  2026-07-17  2384555b6   timestep_control\n\nClassification: migrate silently. It is a rename plus a type change - pid_controller was a bool defaulting to false, timestep_control is an enum - but for the value in use the mapping is exact: pid_controller = true is timestep_control = pid_nonlinear_its. Behaviour is preserved, so this is not one of the changes that must be left alone as part of what is being measured.\n\nConvenient accident: SNES-MUMPS-1 as written already says pid_controller = true, so it applies unchanged to the first nine anchors and needs the substitution only for the last three.\n\nThis matters beyond tidiness because BOUT++ aborts on unused options: today's test2scratch/BOUT.inp names timestep_control and would abort on any of the first nine anchors.","notes":"Scope widened by the probe 2026-07-31: the ledger must cover compile-time build options, not only BOUT.inp options. The limiter is the proof - HERMES_SLOPE_LIMITER=VanAlbada is accepted by CMake at a 2025-08 commit and generates a config header naming a type BOUT++ did not yet define, so the build dies at 82% of the compile with no warning at configure time. Same class of problem as an input option that changed name or default, and worse in kind, because the failure is late and its message names a C++ type rather than the setting that caused it.\n\nSo the ledger has two sections with the same shape: input options and build options, each row saying from which commit a setting is valid and what it was before.\n\nPARENT\n  ↑ ○ so-cnv: (EPIC) perf-bisect: how Hermes-3 performance evolved across development (F5) ● P1\n\nDEPENDS ON\n  → ✓ so-wvw: perf-bisect: does the oldest anchor still build? (window feasibility) ● P1\n\nLaunched rung 1 on 2026-07-31 and it aborted at init: 'Could not find braginskii_collisions'. Three things came out of it.\n\n1. The component list is a harder migration than any option rename, and is 1-to-many. At the oldest anchor the component is a single 'collisions'; today's input names braginskii_collisions, braginskii_friction, braginskii_heat_exchange and braginskii_conduction. hermes-3 313029e1 (2025-09-24) renamed the closure classes and d971b10e (2025-09-29) moved friction OUT of collisions into its own component - 36 files, and it edited the integrated tests' own BOUT.inp files. So this is a decomposition, not a rename, and whether it preserves behaviour is a physics judgement, not something to assume. Get it wrong and the old anchors solve a different model while still producing a plausible wall time.\n\n2. Measured across the ladder, the boundary is clean: the modern component names first appear at the 2025-12 anchor e0f1ebfb. The four anchors from 2025-08 to 2025-11 have only the old 'collisions'.\n\n3. apply_recipe.py already IS a migration ledger, one-directional. Its RETIRED_OPTIONS table rewrote pid_controller into timestep_control unprompted - correct for today, wrong for a 2025-08 build, applied silently. The perf-bisect ledger should extend that table with a from-which-BOUT++-commit dimension rather than being built separately. Until it is, apply_recipe.py cannot be run unsupervised against a historical build.\n\nProcess win worth building into the driver: component availability can be checked statically by grepping each anchor's source for the registered names. Twelve anchors took seconds and needed no builds. The driver should run that precheck before it builds anything, so an input incompatibility costs seconds rather than a build plus a launch.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:45:00Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T12:50:04Z","dependencies":[{"issue_id":"so-8h8","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:16Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-8h8","depends_on_id":"so-wvw","type":"blocks","created_at":"2026-07-31T11:45:21Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-nnt","title":"perf-bisect: choose the anchor commit ladder","description":"Sampling major PR merges is a guess: of 152 merges on master in the last 12 months only 49 touch src/ or include/, and judging which of those matters is what the measurement is supposed to answer. Window confirmed by the user as 12 months, and 12 anchors.\n\nProposed ladder, one per month = the last first-parent commit touching src/ or include/ in that month:\n\n  2025-08  a35e2f5e  2025-08-27  more-diagnostics (#385)\n  2025-09  6c73a474  2025-09-17  fix-mixed-old-new-reactions (#396)\n  2025-10  a9f8f30d  2025-10-29  fix-electromagnetic (#420)\n  2025-11  e027256d  2025-11-21  bump-bout (#427)\n  2025-12  e0f1ebfb  2025-12-15  warnings-clean-check0 (#446)\n  2026-01  f6ae2d1e  2026-01-29  Nonorthogonal minimal differential operators MMS (#285)\n  2026-02  920ba829  2026-03-01  permissions-classical-diffusion (#517)\n  2026-03  e1fe3af7  2026-03-26  hcx-refactor (#398)\n  2026-04  8220ead4  2026-04-24  fix-1d-examples (#553)\n  2026-05  2eeda48f  2026-05-15  fix-doc-equations (#558)\n  2026-06  1084ad65  2026-06-25  fix-communications (#589)\n  2026-07  20581461  2026-07-17  geometry_1d (#232)\n\nNo extra anchors are needed for BOUT++: all seven first-parent submodule bumps in the window (2025-08-19, 2025-11-21, 2025-12-12, 2026-01-28, 2026-05-15, 2026-06-13, 2026-07-07) already fall inside these twelve intervals, and the 2025-11 anchor IS a bump. So every bump is bracketed and attributable to an interval.\n\nTwo things to check before accepting: the 2026-02 anchor carries a 2026-03-01 date (author vs commit date), so confirm it is really the February tip; and the oldest anchor is the feasibility probe's target, so the ladder may lose its early rungs if that build fails.","notes":"2026-09-20 audit: this looks DONE. ladder.toml holds exactly 12 anchor blocks with sha, month, date and subject, matching the proposed ladder, and the MC-only constraint is recorded in its header. 13 rows marked project=perf-bisect were run against it on 2026-07-31 and 08-01. Close unless something is missing that the file does not show.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:44:59Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T16:53:40Z","closed_at":"2026-09-20T16:53:40Z","close_reason":"Audited 2026-09-20 and found already done. ladder.toml holds exactly twelve anchor blocks with sha, month, date and subject, the MC-only constraint is in its header, and thirteen perf-bisect rows were run against it. The new commits.tsv confirms every sha resolves and every date matches git.","dependencies":[{"issue_id":"so-nnt","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:15Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-nnt","depends_on_id":"so-wvw","type":"blocks","created_at":"2026-07-31T11:45:20Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-mmw","title":"perf-bisect: mark history runs in the store so they never mix with tuning rows","description":"perf-bisect rows should live in the same index.tsv - a second store means a second extractor and a second schema drifting apart - but they must be filterable, or every solver-opt comparison silently picks up commits it was never meant to include. Proposal: one new column, project, defaulting to solver-opt. Cheaper than a forked repo and it makes the separation machine-checkable rather than a naming convention. Open question: whether epoch already does enough of this job, and whether extract_test.py should refuse to write a history row without the marker set.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:44:25Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T10:44:25Z","dependencies":[{"issue_id":"so-mmw","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:14Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-9e6","title":"perf-bisect: run area, naming, and separation from solver-opt","description":"Keeping the two projects apart is the user's stated priority. Settled 2026-07-31:\n\n  runs   -\u003e /home/mike/work/cases/perftests/perf-bisect   (follows the perftests convention)\n  clone, worktrees, builds -\u003e /home/mike/work/hermes-bisect  (exclusively perf-bisect; the compile licence is scoped to it)\n\nShared with solver-opt: the results store and the extraction tooling, which already carry hermes_commit, bout_commit, petsc_version, recipe and epoch. Separate: run area, build area, design/log directory under notebooks/perftests, and the campaign script.\n\nNaming needs an extension rather than a reuse: solver-opt deliberately keeps the commit sha out of run names because it is noise there, but here the commit IS the variable and must be in the name.\n\nOpen question: the exact name form, and whether the perf-bisect run area needs its own copy of the grid files (Hermes resolves grid paths relative to the launch directory).","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:44:24Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T17:33:40Z","closed_at":"2026-07-31T17:33:40Z","close_reason":"Run area is /home/mike/work/cases/perftests/perf-bisect, naming is \u003ctest\u003e-\u003cdate\u003e-mc-\u003csha\u003e, and the index's project column separates these rows from solver-opt tuning rows.","dependencies":[{"issue_id":"so-9e6","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:13Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-6p0","title":"Showcase residual notebook cannot be re-executed: analysis.py is gone","description":"residual_norm_postprocessing.ipynb cell 2 does 'import analysis as A' and calls A.ensure / A._cases. perftests/analysis.py no longer exists and was never tracked in git, so it is not recoverable from history — the module the notebook depends on is gone for good. The notebook therefore cannot be run at all, independent of any guard-cell question.\n\nIts stored outputs remain valid: the guard-cell contamination was checked and the shares are identical raw versus masked to the digit on both its cases (MC 86.32/13.46 Pe/Pd, VanAlbada 72.03/26.97), because those two runs hold a denormal in the guards rather than garbage. The reduction code has since been corrected to mask explicitly so it no longer depends on that luck, but the change could not be executed.\n\nFix is to port the loading cell onto the results store and case_comparison, which is where every other analysis now reads from. That also removes the dependency on 2.1 GB case directories that R8 will eventually delete — the notebook currently needs the raw dumps and would die when they go.\n\nReferenced as the showcase for SNES residual attribution in perftests/CLAUDE.md, so it is not an orphan.","acceptance_criteria":"The notebook runs end to end against the results store, with no dependency on a deleted helper module or on raw case directories.","status":"open","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T09:19:56Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T09:19:56Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-9rb","title":"solver_fails records the worst step, not the total","description":"extract.py line 833 sets solver_fails = snes['solver_fails'].max(), while the adjacent nl_its and lin_its use .sum(). The column therefore holds the largest failure count seen in any single output step, not the run's cumulative failures.\n\nFound 2026-07-30 on test5steady under SNES-MUMPS-2: the index row says 7, the log says 661 across 353 reporting steps. Those 661 failures are the entire explanation for that run's cost - each one rejects a step, cuts the timestep and forces a Jacobian rebuild, which is how a 20x100 grid covering 0.5 ms burned 385102 residual evaluations and 638 s. Anyone reading the index alone sees 7 and concludes the run was healthy.\n\nWhether .max() was deliberate is unclear - a worst-burst figure has some value, since the recipe aborts on max_snes_failures consecutive failures. But the name and the neighbouring columns both say cumulative, and the cumulative number is the one that explains cost.\n\nSuggest recording both: solver_fails as the sum, and a separate worst-burst column if the abort margin is worth tracking.","acceptance_criteria":"The index carries the cumulative failure count for a run. If a worst-burst figure is also wanted it occupies its own named column. Existing SNES rows are re-extracted so the column means the same thing throughout.","status":"closed","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T18:45:44Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T23:22:17Z","closed_at":"2026-07-30T23:22:17Z","close_reason":"Fixed 2026-07-31. extract.py now records solver_fails as the sum, matching nl_its and lin_its, and the per-step worst moves to a new solver_fails_max column. Both documented in schema.md. All 13 affected rows recomputed from their stored bundles rather than re-extracted, since several case directories are already deleted: test5steady MUMPS-2 now reads 661 where it read 7, test5stiff 245 where it read 5. 20 extractor tests pass.\n\nThe correction changed a conclusion: test4 is not failure-free, its MUMPS-1 baselines fail 24 and 43 times. What is failure-free is MUMPS-2 on test4, and the run that removes them is scale_vars - across study 1's scan the count goes 24, 24, 23, 0 as it is switched on. On test5 that stops working, which sharpens the open question.","dependencies":[{"issue_id":"so-9rb","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T19:45:50Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-z0r","title":"Recipes are stale against current BOUT++ and abort on startup","description":"Two recipes in the read-only hermes-perftest repo set options that current BOUT++ rejects. Both abort the run at startup, so they cost a full setup-and-launch cycle each time.\n\nSNES-MUMPS-1 sets solver:pid_controller = true. Nothing reads it - renamed to timestep_control, whose default pid_nonlinear_its is the same behaviour - and the perftest inputs set error_on_unused_options = true, so the run aborts. Hit twice: study 1 on test4steady, and again setting up test4stiff.\n\nCVODE-2 sets solver:use_precon = True. Current BOUT++ throws on it outright, pointing at cvode_precon_method. The replacement is NOT the default: cvode_precon_method defaults to none, and the auto value falls through to the petsc preconditioner whenever the model registers none of its own. The faithful equivalent of the old use_precon=true is cvode_precon_method = user, set explicitly. Getting this wrong silently changes which preconditioner CVODE uses, which is the whole subject of the CVODE-beats-SNES puzzle.\n\nBoth were fixed by hand in the case BOUT.inp, so the deviation lands in the diffs column of every affected row and has to be justified in varied each time.\n\nThis also explains part of the epoch problem: the June baselines were run on a BOUT++ that accepted these options. A recipe that no longer applies cleanly is a signal that the code moved underneath the baseline.\n\nThe recipes live in hermes-perftest, read-only for this project, so the fix is not ours to make directly. Options: ask for a commit there, or carry patched copies and record which file was used.","acceptance_criteria":"Applying SNES-MUMPS-1 or CVODE-2 to a fresh case produces an input that runs without a hand edit, or the project has an agreed patched-recipe path and the index records which recipe file was used. The CVODE preconditioner choice is recorded explicitly, not left to auto.","status":"closed","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T18:09:18Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T23:22:19Z","closed_at":"2026-07-30T23:22:19Z","close_reason":"Fixed 2026-07-31 in apply_recipe.py, which now translates retired options as it applies a recipe and says loudly what it did and why. Covers solver:pid_controller -\u003e timestep_control = pid_nonlinear_its, and solver:use_precon -\u003e cvode_precon_method = user or none by value.\n\nThe translation table is deliberately faithfulness-first: an entry must reproduce exactly what the retired option did. A known-retired option carrying an unrecognised value exits with an error rather than guessing, because a wrong translation silently changes what a recipe means and is worse than the abort it prevents. cvode_precon_method is the reason for that rule - its default (none) and its auto value both differ from what use_precon = true meant.\n\nEach rewritten line carries a comment naming the option it replaced, and the tool prints a reminder that the case now differs from its named recipe. Verified on both stale recipes and on SNES-MUMPS-2, which passes through silently. The read-only recipe files were not touched.","dependencies":[{"issue_id":"so-z0r","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-07-30T19:09:37Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-8ac","title":"Option diff reports derived defaults as changes","description":"Study 1's option diff lists solver:pseudo_alpha and solver:pseudo_alpha_minimum as differing between the MUMPS-1 and MUMPS-2 runs. Nobody set either. Their defaults are derived - pseudo_alpha = 100 * atol * solver:timestep, and the minimum is 0.1 * pseudo_alpha - so relaxing atol from 1e-7 to 1e-3 moved them four decades as a consequence. The diff reports one real change three times.\n\nThey are also inert: every run uses equation_form = rearranged_backward_euler, so the pseudo-transient path never executes and pseudo_alpha is never read. They appear in options_used because BOUT++'s SNES solver queries all its options in the constructor's member-initialiser list, before the equation form is known, so an option belonging to a dead code path still records as consulted. param_diff cannot tell that apart from a live setting.\n\nThis recurs on every tolerance change and on any other derived default, so it is noise the reader has to learn to ignore - against R11.\n\nProposed fix: BOUT.settings records 'source: default' per option and the log prints '(default)'. Split the diff on that. Options sourced from an input file are deliberate and stay in the main list; options sourced from a default moved as a consequence and go in a separate collapsed group. General, needs no hand-maintained list of inert options.\n\nOpen question: whether a default-sourced difference is ever worth surfacing on its own - it would be, if two runs used different builds whose defaults differ. Within one build it always traces back to an input that the diff already shows.","acceptance_criteria":"Option diff separates input-sourced differences from default-sourced ones; the MUMPS-1 to MUMPS-2 diff shows atol and rtol as the change, with pseudo_alpha listed as derived rather than as a peer.","status":"closed","priority":2,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T17:42:07Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T23:22:18Z","closed_at":"2026-07-30T23:22:18Z","close_reason":"Fixed 2026-07-31. The log line parser already captured each option's origin marker - 'default', 'user_default', or the input path - and discarded it. Now threaded through options_used(with_defaulted=True) into diff_option_sets, which splits off a derived group: keys that differ but that EVERY case took from a code default. If any case set one deliberately it stays in the main list. The cover names them in one line instead of tabulating them as peers.\n\nOn the MUMPS-1 to MUMPS-2 diff the main list is now the five deliberate changes (atol, rtol, lag_jacobian, max_snes_failures, scale_vars) and pseudo_alpha with pseudo_alpha_minimum are named as derived. Both reports rebuilt and checked.","dependencies":[{"issue_id":"so-8ac","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T18:42:12Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-t4a","title":"Study 1 docstring predates the guard-cell fix","description":"The cover conclusions for test4_mumps1_to_mumps2 say nothing about which equation gates convergence, because when they were written the answer looked like Pe and was guard-cell garbage. It is now answerable: Pd holds 74% of the interior norm on the MUMPS-1 runs, and neither Pd nor Pe on the scale_vars ones - the momentum equations.\n\nAlso unaddressed there: the two tolerance-relaxing configurations do not settle. Their ddt rises again by one to two decades after falling, which the endpoint physics check cannot see. The user's call whether that changes the conclusion.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T16:47:01Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T09:52:21Z","closed_at":"2026-07-31T09:52:21Z","close_reason":"Study 1 docstring updated. It now records which equation gates convergence and that it corroborates the two levers acting differently: baseline Pd 74%/Pe 26%, lag_jacobian leaves it at 71/29 as a path-preserving change should, scale_vars moves it to the momentum equations (NVd 36%, Pe 33%, NVd+ 25%), MUMPS-2 is NVd+ 85%. Interior-only, with a note that pre-2026-07-30 numbers said Pe from guard-cell contamination.\n\nThe non-settling observation is recorded as OPEN rather than folded into the conclusion, since whether it disqualifies the -46% those two configurations bought is your call, not mine.","dependencies":[{"issue_id":"so-t4a","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-30T17:47:02Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-nou","title":"Let the physics-build campaign read the store (R15)","description":"analysis-perftests.py reads case directories, so it cannot use resid_regions, ddt or physics at all, and its reports stop building once R8 deletes the dumps. Three of the solver-opt pages are not solver-specific and would serve it directly: step character, the PETSc event breakdown, and physics evolution.\n\nThe mechanism already exists - the shared cover took 'dirs' and 'case_table' hooks for exactly this. Decide whether that campaign switches to bundles wholesale or per page.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T16:46:58Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T16:46:58Z","dependencies":[{"issue_id":"so-nou","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-30T17:47:01Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-3yz","title":"Physics evolution and monitor-view pages from the store (R15)","description":"Two pages the report was missing, both fed by new bundle tables so they survive R8.\n\nphysics.tsv: the four monitor quantities per output - Ne/Te at the outer midplane separatrix, and the target maxima. Target maxima use the store's own region selector rather than the monitor's index arithmetic, so the history and the recorded endpoint scalar are the same quantity; they differ by 0.2%, which is a guard cell this dataset does not carry. Separatrix columns are named ne_omp_sep/te_omp_sep because this is a connected double null and the index deliberately records no unqualified ne_sep.\n\nddt.tsv gained rms_ddt_vw and conversion so the rate of change can be drawn the way cmonitor draws it, volume weighted and normalised.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T16:20:44Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T16:20:46Z","closed_at":"2026-07-30T16:20:46Z","close_reason":"Both pages built; physics sits after the evolution page, the monitor view before the relative-rate page.","dependencies":[{"issue_id":"so-3yz","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T17:20:45Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-99c","title":"Time-derivative history from a new bundle table (R15)","description":"Nothing in the report showed how fast the solution was still moving, which is not answerable from the residual: the residual says how well the implicit step was solved, ddt says how much the step changed.\n\nNew ddt.tsv in the bundle: RMS d/dt per evolved variable per output, unweighted, interior cells only (F20 applies to these fields too), with rms_state beside it so a relative rate can be formed. Pointwise ddt(v)/v deliberately not recorded - momentum passes through zero.\n\nTwo pages, absolute and relative, plus simulation speed and the Krylov-per-Newton ratio added to the evolution page.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T15:52:22Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T15:52:24Z","closed_at":"2026-07-30T15:52:24Z","close_reason":"Built and re-extracted; the loose-tolerance and scale_vars runs show a rate that oscillates rather than settling, which is new information about what those settings cost.","dependencies":[{"issue_id":"so-99c","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T16:52:23Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-dur","title":"Compile-time options on the report cover (R17)","description":"The cover named the code but not how it was built. Added: limiter, conduction method and build type to the CASES table.\n\nTwo of the three cannot come from a case directory at all. Conduction method has no dump or log constant (so-xnq), so it exists only where it was declared in the index; CMake records no build type anywhere in the run, so the extractor now reads it back from the 'Compiled with flags' line and stores it as build_type.\n\nThe cover for this campaign now reads the bundle rather than the case directory for everything else too, which is what the campaign always claimed to do and did not.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T15:35:53Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T15:35:55Z","closed_at":"2026-07-30T15:35:55Z","close_reason":"Done: build_type captured and documented in the schema, index schema can now grow a column without a migration, cover reads the store.","dependencies":[{"issue_id":"so-dur","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-30T16:35:54Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-it4","title":"Three campaign pages from bundle data already captured (R15)","description":"The study-1 PDF plots only a fraction of what the bundles hold. Three pages proposed and approved by the user:\n\n1. Unit cost of identical work: seconds per Jacobian build (MatFDColorApply time / n_jac_builds) beside seconds per RHS evaluation. Separates a solver change from a machine-state change - the first normaliser is stable across configurations, the second is not.\n2. What the RHS evaluations were: split ncalls into Jacobian colouring probes and real residual evaluations, probes per Jacobian build, and the fine-grained PETSc event decomposition inside SNESJacobianEval and PCSetUp.\n3. Step character: nonlinear iterations against timestep, marked by SNES converged reason, plus step counts per exit reason.\n\nOpen question: page 2's third panel overlaps scan_cost_breakdown. Decide after seeing both whether the coarse page is still earning its place.","status":"closed","priority":2,"issue_type":"task","assignee":"Mike Kryjak","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T13:50:31Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T14:59:58Z","started_at":"2026-07-30T13:50:36Z","closed_at":"2026-07-30T14:59:58Z","close_reason":"Three pages built, then cut to what varies: the coarse and fine cost breakdowns were identical bars so only the fine one survives, and the probes-per-Jacobian panel was a constant of the discretisation. Region page added on top of a new bundle table. Guard-cell contamination found and fixed in the extractor along the way (F20).","dependencies":[{"issue_id":"so-it4","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-30T14:50:35Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-lxa","title":"Why did one run stall 68 s in MPI comms? (F17)","description":"Two identical MUMPS-1 runs on test4steady did the same work to 1.8% and differed 34% in wall clock. Measured: the slow one spent 80.2 s in MPI communications against its twin's 12.1 s — 33.5% of its wall against 6.8%. That 68 s is the whole difference, and it made each RHS evaluation cost 3.88 ms instead of 2.59 ms.\n\nTwo explanations are already dead. Slot placement: step2-lagjac ran in the same slot 3 with comms at a normal 5.3%. Loading: the slow run was the LEAST loaded of the six, averaging 2.02 concurrent runs against the others' 3.\n\nMatters because it is the size of every effect we are trying to measure, and because it is invisible unless wtime_comms is checked — wall clock alone just looks like noise.\n\nWhat differed: the slow run was the only one spanning 85-324 s, so it ran through the tail when the other slots emptied. Worth testing whether comms degrade when the machine empties (MPI busy-wait, frequency/C-state behaviour) rather than when it fills, which is the opposite of the usual assumption. Cheap test: the same case alone versus three-up, comparing wtime_comms rather than wall.","acceptance_criteria":"Either a mechanism for the slot difference, or a measurement showing it is not reproducible.","notes":"2026-09-20 audit: leftover from the 2026-08 tuning campaign archived on 2026-09-18. Eleven sibling issues were closed that day with the reason 'Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build'. This one was left open by omission, not by decision. It should either be closed the same way or re-justified against the new campaign. The user decides. Note that so-yjs.2 is NOT in this group: it was updated on 2026-09-19 and its conclusion graduated into SNES-MUMPS-3, so it is live.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T12:45:58Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:15Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-ab0","title":"Compute noise from repeat runs in the index (C2)","description":"A test's noise is derived, never stored: rows sharing test, recipe, varied, hermes_commit, limiter, conduction_method, check_level, cores, decomposition and concurrency are repeats of one configuration, and the spread across them is the figure.\n\nNeeds a function in sdtools perftest/ that groups the index by that key and reports relative spread per metric (wall_s, ncalls, nl_its, lin_its at minimum), plus the count of samples behind each figure so a one-sample group cannot masquerade as a measurement.\n\nFeeds the results table: every comparison shown should carry the error bar this produces, and per C4 a difference below it is reported as no measurable effect.","acceptance_criteria":"A function that turns the index into per-configuration spreads per metric, with sample counts, used by the results table.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T12:22:13Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T12:45:57Z","closed_at":"2026-07-30T12:45:57Z","close_reason":"Superseded in part and reshaped: the concurrency column it would have consumed was itself wrong. Fixed in perftest/index.py — concurrency is now the time-weighted mean number of simultaneous runs, not a count of distinct overlapping ones, which reported 5 on a three-slot machine and made a lightly loaded run look heavily loaded. Verified against an independent calculation on the six-run scan (2.99/2.99/3.00/2.67/2.96/2.02). The grouping-and-spread function is still wanted and is folded into the results table work.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-yjs.3","title":"Why is snes_fd_color_use_mat never used by PETSc? (F12)","description":"options_left reported snes_fd_color_use_mat as set but never used. It appears in SNES-1, SNES-MUMPS-1 and SNES-MUMPS-2, so every run of the reference recipe has carried a setting that does nothing. Determine whether the option name is wrong or deprecated in PETSc 3.23.3, whether it is simply irrelevant when matrix_free_operator=false, or whether the intended behaviour is being lost. If it was meant to change how the Jacobian is coloured, its absence may matter for the Jacobian-reuse hypothesis in so-yjs.1.","acceptance_criteria":"A statement of what the option was meant to do, why PETSc ignores it, and whether the recipes should set something else instead.","notes":"2026-09-20 audit: leftover from the 2026-08 tuning campaign archived on 2026-09-18. Eleven sibling issues were closed that day with the reason 'Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build'. This one was left open by omission, not by decision. It should either be closed the same way or re-justified against the new campaign. The user decides. Note that so-yjs.2 is NOT in this group: it was updated on 2026-09-19 and its conclusion graduated into SNES-MUMPS-3, so it is live.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T16:48:02Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:15Z","dependencies":[{"issue_id":"so-yjs.3","depends_on_id":"so-yjs","type":"parent-child","created_at":"2026-07-29T17:48:02Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-yjs.2","title":"Fix the SNES timestep controller settings (F10)","description":"The PID timestep controller on SNES-MUMPS-1 and -2 cannot ask for a smaller timestep. facP = (target_its/nl_its)^kP (snes.cxx:1684); both recipes set target_its=100 with max_nonlinear_iterations=10, so the ratio is always \u003e= 10 and every successful step multiplies dt by at least 1.71, clamped only at 5.0 (snes.cxx:1691). max_timestep defaults to 1e37 (snes.cxx:221) and no recipe sets one. On failure dt is halved and the run aborts after max_snes_failures (10, or 20 in MUMPS-2). pid_consider_failures defaults false (snes.cxx:294) and no recipe enables it, so the back-off logic never runs.\n\nThree one-line experiments, each cheap on a short test and each isolating one part in the R3 sense:\n1. target_its below max_nonlinear_iterations — e.g. target 5 with max 10 — so the controller can go both ways.\n2. a real max_timestep, rather than the 1e37 default.\n3. pid_consider_failures = true, so growth is damped after failures.\n\nAlso worth testing timestep_control = residual_ratio, which the user reports was tried briefly without success — it deserves proper attention rather than a quick look.\n\nExpected to matter most where the solver is stiff and dt cannot safely grow, which is where SNES has failed. Do not run these until the diagnostics work is done (R10) — without convergence reasons and iteration counts captured, a failure cannot be attributed.","acceptance_criteria":"Measured effect of each of the three changes on a short test, with the controller's behaviour visible in the captured iteration counts rather than inferred.","notes":"MEASURED 2026-08-01 on test5steady, as step 1 of the CVODE-like scan. target_its 100 -\u003e 5, against max_nonlinear_iterations = 10, everything else held:\n\n                    wall    ncalls   nl_its  fails  jac builds\n  baseline          676 s   438333    1007    731     2739\n  target_its = 5    432 s   279790    1210    311     1748\n                    -36%     -36%     +20%   -58%     -36%\n\nPhysics unchanged: peak target Te 131.0407 against 131.0465, 0.004%. Well clear of any plausible scatter (see so-xi1). This alone puts SNES 2.9x ahead of CVODE-2 on this test, against 1.8x before.\n\nNote what did NOT happen, because it changes what is left to do. Nonlinear iterations went UP, not down: the run takes more accepted steps because dt now grows by 1.44 per success instead of 2.92, and the gain comes entirely from those steps succeeding rather than failing.\n\nAnd the controller is still not two-way in practice. 576 of the accepted steps still converge in ONE Newton iteration, and at nl_its = 1 the factor is 5^0.233 = 1.44, still above one. So dt still only ever grows; the change made it grow gently rather than made it able to shrink. Genuinely making the controller responsive needs nl_its to rise above target, which is what tightening the tolerance would do - so steps 1 and 3 of this scan interact, exactly as the note on so-hgc warned.\n2026-09-19: SNES-MUMPS-3 (new recipe in hermes-perftest) sets target_its = 7 against max_nonlinear_iterations = 10, so a step taking 8-10 iterations now shrinks dt. It also adds mat_mumps_icntl_24 = 1. It is the starting recipe for the parent runs. max_timestep and pid_consider_failures remain untested.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T16:43:06Z","created_by":"Mike Kryjak","updated_at":"2026-09-19T16:26:09Z","dependencies":[{"issue_id":"so-yjs.2","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:55Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-yjs.2","depends_on_id":"so-yjs","type":"parent-child","created_at":"2026-07-29T17:43:06Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-r53.6","title":"Design derived residual metrics to replace storing resid_ fields","description":"The resid_\u003cvar\u003e fields are the only part of the core capture set with real disk cost — seven 2D fields per output step. They are not compressed at write time; they stay on during the run and get reduced at extraction, before dumps are deleted (R8). This issue designs that reduction.\n\nCandidates, from what the residual-norm showcase notebook actually uses: per-equation share of total residual sum-of-squares (seven dimensionless numbers, comparable across time and cases); snes_global_residual (already scalar); location of the largest snes_local_residual cell as grid index or R-Z; share of the norm in named regions (core, SOL, targets), possibly time-averaged rather than per step.\n\nTarget is order ten to twenty numbers per output step instead of seven fields. Test whether that set actually supports the analyses we want — the honest check is whether a question previously answered from the full fields can still be answered from the reduction alone. SNES only; CVODE writes no equivalent (F8).\n\nPrior art: /home/mike/work/notebooks/perftests/residual_norm_postprocessing/residual_norm_postprocessing.ipynb, including its caveats (guard cells exactly zero, no volume weighting, shares not magnitudes, end-of-step snapshots, normalised units).","acceptance_criteria":"A defined metric set, plus a demonstration that it answers at least one question previously answered only from the full fields.","notes":"Scope narrowed by the user 2026-07-30: the output is scalars only. No residual field is stored outside the dump in any format, and snes_global_residual is recorded on every SNES run including under scale_vars, accepting that the weights are unrecorded (var_scaling_factors never reaches the dump; only a 'Var scaling: min -\u003e max' log line per rescale event, snes.cxx:643-651) so it cannot be un-weighted later. What remains to design is which scalars to compute, not whether to keep fields.\n\nConstraint on sequencing, not a blocker: widening the metric set is free while a dump exists and costs a re-run once R8 has deleted it. So dumps extracted before this issue settles should not be cleared unless the run is cheap to repeat.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T14:10:45Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T13:27:09Z","closed_at":"2026-07-30T13:27:09Z","close_reason":"Designed and implemented. The metric is the one the residual-norm notebook already established: per-equation sum of squared residuals over the domain, as a share of the total, no volume weighting (the solver's norm is a plain sum over state-vector entries) and shares rather than magnitudes (raw residual scales with the adaptive timestep). Seven numbers per output step in place of seven full fields.\n\nThe extractor now writes them into series.tsv as share_\u003cequation\u003e plus resid_ss_total, so they survive R8 deletion, and the study report has a page showing which equation holds the solver up over the run.\n\nSampling limit found and annotated on every panel: the resid_ fields refresh once per INTERNAL solver step, not per output. On the fast configurations resid_ss_total takes only 2 distinct values across 91 outputs, against ~70 on the slow ones — so the diagnostic's real resolution collapses exactly when the timestep grows. Anyone reading the history needs the distinct-state count beside it, which the page prints.","dependencies":[{"issue_id":"so-r53.6","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T15:10:45Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-yjs.1","title":"Why does the nonlinear solve fail 661 times on test5 and never on test4?","description":"RESCOPED 2026-07-31. The original question was why a near-unpreconditioned CVODE beats SNES on test5. That premise is dead: F23 re-ran both at one build against each test's own baseline and SNES-MUMPS-2 is fastest on all four short tests, test5s included. The old belief came from comparing runs across different builds and months.\n\nThe question that survives is sharper. MUMPS-2 fails 661 times on test5steady and 245 on test5stiff, and not once on either test4. Each failure rejects the step, cuts the timestep and forces a Jacobian rebuild, so 0.5 ms of test5steady takes 1131 internal steps and 2406 Jacobian builds where test4steady takes 16 and 23. At ~160 evaluations per coloured build that is essentially the entire cost. Removing the failures is the whole prize.\n\nRULED OUT so far:\n- Problem size and cost per evaluation. test5 is HALF the grid of test4 (20x100 against 40x100) and its residual evaluations are three times cheaper.\n- scale_vars being absent. It is what removes the failures on test4 — across study 1 the count runs 24, 24, 23, then 0 the moment it is switched on — and test5 has it on and still fails 661 times.\n- The gating equation. Momentum-gated does not mean failure-prone: test4steady MUMPS-2 is NVd+ 85% with ZERO failures, while test5steady MUMPS-2 is NVd+ 98% with 661. test4stiff MUMPS-2 is Pe 99% with zero, test4steady MUMPS-1 is Pd 74% with 24. Gating equation and failure count are independent, so 'the momentum equation is what breaks it' is not the answer.\n\nREMAINING, in order:\n1. The timestep controller (F10). On the MUMPS recipes it is structurally unable to reduce dt — target_its=100 against max_nonlinear_iterations=10 means every success multiplies dt by at least 1.71 with no ceiling, while failure only halves it. Growth outpaces recovery, so dt is driven into the failure boundary and stays there. This predicts exactly the observed pattern: a run that fails continuously without ever diverging. Cheapest test in the project, and it is already scoped as so-yjs.2 — that issue is now the direct route into this one and should be done first.\n2. If the controller fix does not do it: what about test5's state makes a Newton step of that size fail where test4's does not. Only worth opening once 1 is settled, since the controller is currently choosing the step sizes being tested.\n\nDo not re-open this as a CVODE-versus-SNES question without new evidence.","acceptance_criteria":"A hermes-oracle report naming the cause, with evidence isolating it from the other three hypotheses, and a concrete route to SNES settings that match or beat CVODE on test 5.","notes":"2026-09-20 audit: leftover from the 2026-08 tuning campaign archived on 2026-09-18. Eleven sibling issues were closed that day with the reason 'Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build'. This one was left open by omission, not by decision. It should either be closed the same way or re-justified against the new campaign. The user decides. Note that so-yjs.2 is NOT in this group: it was updated on 2026-09-19 and its conclusion graduated into SNES-MUMPS-3, so it is live.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:53:10Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:14Z","dependencies":[{"issue_id":"so-yjs.1","depends_on_id":"so-dai","type":"blocks","created_at":"2026-07-30T11:38:59Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-yjs.1","depends_on_id":"so-f4m","type":"blocks","created_at":"2026-07-30T11:38:59Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-yjs.1","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:54Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-yjs.1","depends_on_id":"so-yjs","type":"parent-child","created_at":"2026-07-29T14:53:10Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-yjs.1","depends_on_id":"so-yjs.2","type":"blocks","created_at":"2026-07-31T10:52:03Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":4,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-tg6","title":"Non-solver factors","description":"Things outside PETSc that move performance and must be held fixed or understood: slope limiter and scale_vars (F1), and how performance drifts across Hermes-3 commits (F5).","status":"open","priority":2,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T12:34:52Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T12:34:52Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-csm","title":"Process and provenance","description":"How runs happen and get recorded repeatably: the run lifecycle skill (R22), beads conventions (R13), and the version bump / rebaseline procedure (R18).","status":"open","priority":2,"issue_type":"epic","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T12:34:51Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T12:34:51Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-65o","title":"Understand performance of non-PETSc developments: slope limiter, scale_vars (F1)","description":"Non-PETSc settings move test times by more than most solver settings do — at fixed commit 731e6449 and CVODE-2, the slope limiter changed test5scratch by 1.7x and the conduction method by up to 32%. Policy: assume such settings are held fixed, but a short list must be actively understood because they are under development and will change under us. Currently slope limiter and scale_vars; expect additions. Deliverable is a performance characterisation of each, enough to know which build a solver result is valid on.","status":"open","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T12:03:10Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T12:35:38Z","dependencies":[{"issue_id":"so-65o","depends_on_id":"so-f4m","type":"blocks","created_at":"2026-07-29T13:35:27Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-65o","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:56Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-65o","depends_on_id":"so-tg6","type":"parent-child","created_at":"2026-07-29T13:35:24Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-uu0","title":"Record the slot/concurrency confound and measure slot-to-slot difference (E3, R20)","description":"Findings entry E3: which of the three 10-core slots a run occupied was only ever recorded as free text in the ledger note field, and the conduction-method A/B compared slot s1 against slot s2. If slots differ in speed — plausible given core-to-memory distance — part of that measured difference is the slot. Measure slot-to-slot difference alongside the run-to-run scatter in so-f4m, and make slot and concurrency real columns in the store (so-nh3).","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T11:33:35Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T12:03:29Z","closed_at":"2026-07-29T12:03:29Z","close_reason":"Superseded by so-f4m, which now covers concurrency penalty, slot-to-slot difference and run-to-run scatter in one experiment.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-62q","title":"Write the solver-opt run lifecycle skill (R22)","description":"R22 requires the repeatable mechanics to live in a written skill rather than per-session improvisation: apply recipe, launch, gate, extract record, validate, then delete dumps. Composes with the existing hrun skill for setup/build-check/launch/gate — inherit that gate, do not restate it — and adds what is specific to this project: epoch tagging (R18), the expanded record (R15/R17), capture-before-delete ordering, and the three-slot concurrency limit (R21). Hard rule to encode: dumps are deleted only after extraction is validated, never before, and never for a run whose record failed validation.","acceptance_criteria":"A skill file in this project that an agent can follow end to end, deferring to hrun where hrun already covers the step.","notes":"Definition of a test, from the user, 2026-07-29: copy the base restart files into the test directory overwriting what was there, run once, capture the output. That is the whole test. Anything run afterwards is a different test, not a continuation. Two rules follow for the skill: a run that dies and is resumed is a different test and must not be recorded as the original; and capture must happen before the directory is reused, since the next test overwrites it. This also removes the BOUT.log-overwrite worry — the log is complete for the test it belongs to.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T11:15:10Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T18:12:53Z","closed_at":"2026-07-31T18:12:53Z","close_reason":"Written as .claude/skills/solver-opt-run/SKILL.md in this project. Composes with hrun and inherits its gate rather than restating it; carries the definition of a test, the open-row-before-launch ordering, extract-validate-then-delete, epoch tagging, the three-slot concurrency rule, the failure taxonomy and the settled crawl abort trigger.","dependencies":[{"issue_id":"so-62q","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-07-29T13:35:23Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-0zl","title":"Abort criteria for crawling runs (R19)","description":"R19's 'crawl' failure class is a run so slow it must be aborted, which needs a written trigger so aborting is mechanical rather than a judgement call each time. Candidate triggers: simulation speed (ms simulated per 24h wall, already in cmonitor row 2) falling below a set fraction of the reference recipe on the same test; projected completion time exceeding K times the reference; internal timestep collapsing below a floor. Also decide what gets recorded for an aborted run so it is still a usable data point under R19.","acceptance_criteria":"A trigger a monitoring script can evaluate unattended, plus the record written on abort.","notes":"Settled for perf-bisect 2026-07-31 and implemented in run_ladder.py; generalise from here rather than re-deciding.\n\nThe trigger is a stall, not a wall-clock cap: abort when no output step has been written for 2 h. Progress is read from the dump file mtimes, not the log, because a dump advances exactly once per output step in every BOUT++ across the window while a stalling run keeps writing solver-step lines indefinitely. Confirmed on the oldest anchor: its dumps froze at 13:57 with 0 of 200 outputs while BOUT.log.0 kept growing until 16:18.\n\nA plain wall-clock cap was rejected: it cannot tell a stalled run from a merely slow one, and would kill an old anchor that is genuinely completing. A generous 6 h total backstop is kept for a run that does produce output but far too slowly.\n\nRecorded on abort as outcome=crawl_aborted. Extraction is attempted anyway - a partial run still yields snes_steps and steps tables showing the timestep collapse - and the case directory is kept if extraction does not validate.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T10:46:53Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:34Z","closed_at":"2026-09-18T12:42:34Z","close_reason":"Answered by the 2026-09-18 design: wall clock ranks with counts as diagnostics, correctness is a two-level check, the test ladder replaces the fixed sequence, and the cutoff rule replaces the crawl criterion. See design.md and candidates C10-C14.","dependencies":[{"issue_id":"so-0zl","depends_on_id":"so-f4m","type":"blocks","created_at":"2026-07-29T13:35:26Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-0zl","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-29T13:35:21Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-dai","title":"Decide primary ranking metric: RHS evaluations vs wall clock (R15)","description":"R15 records both wall-clock time and total RHS evaluations for every run, but not which one ranks recipes. Case for RHS count: machine-independent, no node-contention noise, exactly reproducible, changes only when solver settings or Hermes-3 change. Case against: it prices only one term of the cost. Settings families differ in linear-algebra work per RHS eval — a direct-LU preconditioner (MUMPS) does heavy factorisation with few RHS evals, so ranking MUMPS against a matrix-free Krylov recipe on RHS count alone flatters MUMPS. That matters because R3 makes SNES-MUMPS a reference technique. Likely resolution: wall-clock is the objective, counts (RHS evals, SNES iterations, KSP iterations, Jacobian builds, PC setups) are the noise-free explanatory decomposition, and a calibrated cost model turns counts into a predicted machine-independent time. Decide before the first wave.","acceptance_criteria":"A stated ranking rule, including how recipes from different preconditioner families are compared.","notes":"Largely answered 2026-07-30 by F17. Two identical MUMPS-1 runs differed 34% in wall clock (179 s slot 1, 239 s slot 3) while doing the same work to 1.8%. Loading points the wrong way — the slow one was the less loaded. So wall clock carries a large slot term that counts do not, and the rule follows: rank on work counts whenever runs span slots, and only use wall clock for comparisons pinned to a single slot. Counts are not noise-free (F16, ~2%), they are just free of the slot term. Still open: how to compare across preconditioner families, where counts price only one term of the cost (F4).","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T10:46:52Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:33Z","closed_at":"2026-09-18T12:42:33Z","close_reason":"Answered by the 2026-09-18 design: wall clock ranks with counts as diagnostics, correctness is a two-level check, the test ladder replaces the fixed sequence, and the cutoff rule replaces the crawl criterion. See design.md and candidates C10-C14.","dependencies":[{"issue_id":"so-dai","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-29T13:35:20Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-dai","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:58Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"so-joa","title":"Define what \"physics results stay reasonable\" means (R15)","description":"R15 now fixes the shape of the correctness check: four quantities, taken at the end of the test, compared against a reference — outboard-midplane separatrix Ne and Te, and maximum target Ne and Te (cmonitor top row under default flags; note --neutrals swaps these for neutral quantities, so the list is named explicitly). Tolerance tightens with test length: loosest on short tests, tighter on scratch, very tight on a run to convergence. What remains: the actual tolerance numbers for each of the three tiers, and which run serves as the reference for each test.","acceptance_criteria":"Three tolerance numbers (short/scratch/convergence) and a named reference run per test, applied by a script with no human judgement.","notes":"Concrete input from the extractor (2026-07-30): ne_target_max and te_target_max are computed and recorded now — max over the 'targets' poloidal region at full radial extent, at the last output step, in SI. Verified on test4steady: Te 12.06 eV, Ne 1.85e20 m^-3.\n\nne_sep and te_sep are NOT, and need a decision from you. The tests are connected double null, so there are four midplanes (inner/outer x lower/upper) and 'the separatrix value' has no single meaning until one is named. The extractor leaves both empty and says so rather than picking one, since a quietly-chosen location would be compared across runs as though it were the agreed one.\n\nAlso still open per schema.md: verdict stays 'no_reference' until there is a rule for choosing which test_id to compare against, so max_dev and reference_id are unfilled.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T10:26:40Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:34Z","closed_at":"2026-09-18T12:42:34Z","close_reason":"Answered by the 2026-09-18 design: wall clock ranks with counts as diagnostics, correctness is a two-level check, the test ladder replaces the fixed sequence, and the cutoff rule replaces the crawl criterion. See design.md and candidates C10-C14.","dependencies":[{"issue_id":"so-joa","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-29T13:35:19Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-joa","depends_on_id":"so-s0b.1","type":"blocks","created_at":"2026-07-29T17:55:59Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-nh3","title":"Results store design (R6/R7/R8): hundreds of runs loadable into context, dump-independent","notes":"Legacy data: the 18 pre-existing ledger rows are preserved in git at perftests/solver-opt/run_records.csv and are the evidence behind findings F1, F2 and F5. The user expects to start the record over once the store is redesigned — decide whether those rows migrate into the new schema or are kept only as an archived reference.\nDecision 2026-07-29: migrate the 18 legacy rows into the new schema rather than archiving them. Fields the old rows cannot supply (RHS counts, cores, CHECK, slot, outcome, epoch) are recorded as unknown, not guessed.\nDesign direction agreed 2026-07-29. Two tiers, separate repo.\n\nMeasured on a real scratch run (test5scratch-2026-07-12-...-va-harmonic, 956 MB total): dumps 954 MB, BOUT.log.0 80 KB / 1151 lines, BOUT.settings 48 KB, BOUT.inp 8 KB, restarts 1.1 MB, all ten rank logs 800 KB. Input + settings + rank-0 log compress to 22 KB; allow ~100 KB per test with diagnose=true and a log_view report. A thousand tests is therefore ~100 MB.\n\nTier 1, the index: one row per test, scalars only — identity, settings, epoch, outcome class, cost numbers, correctness verdict. This is what loads into context under R6, so it stays narrow.\n\nTier 2, the bundle: a directory per test holding BOUT.inp, BOUT.settings, BOUT.log.0, the captured console (log_view), the extracted time series and the derived residual metrics. ~270 KB raw. Stored uncompressed as plain directories, not archives: they are write-once so git packs them well, and leaving them readable means an agent can grep across every run without unpacking. Keep rank 0's log only — the other nine are near-duplicates and account for 720 KB of the 800.\n\nStorage: a separate repo, not the notebooks repo. Name, location and whether it gets a private GitHub remote still to be decided.\n\nDumps: none kept by default (R8 amended). Final restart files kept for any run that seeds another test.","status":"closed","priority":2,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-27T08:54:51Z","created_by":"Mike Kryjak","updated_at":"2026-07-30T10:37:52Z","closed_at":"2026-07-30T10:37:52Z","close_reason":"Design agreed and written down: two tiers (narrow index, per-test bundle), separate private repo at /home/mike/work/perftest-results, no dumps kept, rank-0 log only, bundles as plain directories. Sizes measured on a real scratch run. All of it now lives in that repo's README.md and schema.md, and the extractor is built against it. Remaining work is execution rather than design — creating the index and migrating the 18 legacy rows — which is its own issue.","dependencies":[{"issue_id":"so-nh3","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-29T13:35:17Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"so-6kr","title":"The runner writes its log into the store repo","description":"The runner writes runner.log into the campaign directory, which is inside the store repo. That is the same fault that was just fixed for report PDFs: generated output does not belong in the record repo, which is kept deliberately tiny.\n\nSeen on the first test drive, 2026-09-20: solver-opt-store/campaigns/test-drive/runner.log, 8 KB after four short runs. A real campaign runs for days, so this grows without limit inside a git repository whose history was purged to keep it small.\n\nIt was left untracked for the test drive, which makes it clutter in git status rather than a fix.\n\nWhere it should go is the same place the reports went: the data directory. Note that the campaign's index.tsv and config are a different matter — those are the record and arguably belong in the store. Only the log is output.","acceptance_criteria":"A campaign run leaves no generated log inside solver-opt-store. git status in the store is clean after a campaign finishes.","notes":"2026-09-20, partly overtaken: campaigns/ is now gitignored in the store, so this log no longer reaches GitHub and no longer dirties git status. What remains is smaller and is a matter of tidiness, not exposure: the runner still writes a growing log inside the store directory rather than beside the cases in the data directory. Repriced to P3.","status":"open","priority":3,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T18:21:03Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T19:37:11Z","dependencies":[{"issue_id":"so-6kr","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T19:21:10Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-z78","title":"The cli path comment is stale, and packaging has no install target","description":"Each of the five cli tools carries the comment 'This repository ships no packaging; cli/ is on $PATH and its tools are run by name, so each puts the repository root on the path itself.' The first clause is now false: pyproject.toml exists and declares five console scripts.\n\nThe rest of the comment is still exactly right, and the sys.path.insert line under it is load-bearing. Checked 2026-09-20: solveropt_cli imports in neither the pyenv default python nor the hermes3 spack view, and .bashrc line 171 puts cli/ on PATH. Remove the line and every tool breaks.\n\nTwo things to settle.\n\n1. Correct the comment in all five tools so it says packaging exists but nothing is installed, and the path line is therefore required. Small, but it is five files and it needs the authorisation phrase.\n\n2. Decide what the console scripts in pyproject.toml are for. Installing into the hermes3 spack view is forbidden by standing rule, so the installed names cannot be the local path. Either they serve another machine or a collaborator, or the scripts block is aspirational and should say so. The entry points were made to work correctly regardless (see the closed console-refusal issue), so nothing is broken either way.","acceptance_criteria":"All five cli tools carry a comment that says packaging exists but nothing is installed, so the path line is required. The sys.path.insert lines are still present, because removing them breaks every tool. pyproject.toml's console scripts either have a stated purpose or are marked as serving another machine.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T17:19:06Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T18:05:27Z","dependencies":[{"issue_id":"so-z78","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-20T18:19:09Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-7is","title":"Report generator writes into the store directory","description":"analysis-solver-opt.py writes its PDF to analysis/output/ inside the store repository. That directory is now gitignored, so nothing is committed, but the file still lands inside a git working tree that is supposed to hold only the whole-project record.\n\nThe archived PDFs were moved to $data/reports/archive-2026-08-tuning/ on 2026-09-20. The generator should write there too, under $data/reports/, so that generated output lives with the case data it came from and the store directory holds nothing per-campaign.\n\nSmall change: the Campaign object takes an outdir. Resolve it from the data directory the way perftest/store.py now resolves the bundle root.","status":"closed","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T16:45:00Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:32:45Z","closed_at":"2026-09-20T17:32:45Z","close_reason":"Delivered 2026-09-20 and committed. Extractor: six outcomes decided from the log and the dump, divergence tested before the clock, an interrupt never read as a crash, ms_per_24h written only for a completed run; CVODE counts now come from the dump's cumulative counters, last value not summed, verified against both CVODE rows. Campaign config and runner: campaign.toml with an enforced approval record, the section 8 loop, resumable from the index, cutoff set only by completed runs. Reports: an outcome page before any timing, DNF for a blank wall clock, the convergence-test bar shown as each run's own share with the count kept as a label, PDFs written to the data directory. 136 tests pass. Two gaps filed separately: the runner's kill outcome never reaching the index, and the launch call this workstation blocks.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-810","title":"Step-character panel is unreadable when one run dominates","description":"On the right panel of the step-character page, which counts steps by convergence reason, test5's parent run has about 1.1e5 internal steps while every other run has a few thousand. The axis is therefore set by test5 and the other seven runs render as invisible slivers.\n\nSeen on 2026-09-20 in analysis_01_mumps3_cost_breakdown.pdf, page 10.\n\nOptions: a log axis, normalising each run to its own total, or splitting the panel by run length. Normalising is probably right, since the question the panel answers is which convergence test ended the step, a proportion rather than a count.","status":"closed","priority":3,"issue_type":"bug","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T16:45:00Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T17:32:45Z","closed_at":"2026-09-20T17:32:45Z","close_reason":"Delivered 2026-09-20 and committed. Extractor: six outcomes decided from the log and the dump, divergence tested before the clock, an interrupt never read as a crash, ms_per_24h written only for a completed run; CVODE counts now come from the dump's cumulative counters, last value not summed, verified against both CVODE rows. Campaign config and runner: campaign.toml with an enforced approval record, the section 8 loop, resumable from the index, cutoff set only by completed runs. Reports: an outcome page before any timing, DNF for a blank wall clock, the convergence-test bar shown as each run's own share with the count kept as a label, PDFs written to the data directory. 136 tests pass. Two gaps filed separately: the runner's kill outcome never reaching the index, and the launch call this workstation blocks.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-4jr","title":"perf-bisect ladder spans 10.6 months, not twelve","description":"The study describes itself as covering twelve months of development. The anchor dates say otherwise, measured from the new commits.tsv and confirmed independently: 2025-08-27 to 2026-07-17 is 324 days, about 10.6 months, across eleven distinct calendar months.\n\nFebruary 2026 has no anchor at all. March 2026 has two, 920ba829 on 2026-03-01 and e1fe3af7 on 2026-03-26, just 25 days apart. The ladder labels 920ba829 as February, and its own comment already flags that attribution as needing confirmation.\n\nSo the coverage has a real February hole while two anchors sit almost on top of each other. The largest genuine gap is 45 days, 2025-12-15 to 2026-01-29.\n\nThe user decides: either move the February anchor to a genuinely February-dated commit, or restate the study's reach as roughly eleven months, August 2025 to July 2026. Nothing was changed in ladder.toml.\n\nOne smaller thing found at the same time: ladder.toml abbreviates f6ae2d1e's subject. Git says 'Nonorthogonal with minimal differential operators MMS test. (#285)'; the ladder says 'Nonorthogonal minimal differential operators MMS (#285)'. commits.tsv carries the true subject.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-20T15:56:07Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:56:07Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-0l5","title":"Residual-target objective for pseudo-transient methods","description":"For steady-state work the honest objective is work to reach a residual level, not work to reach a fixed plasma time. The window ladder cannot express that. Needed before pseudo-transient continuation can be tested fairly. The residual history is already recorded per output step.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:47Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:41:47Z","dependencies":[{"issue_id":"so-0l5","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:05Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-dzy","title":"Choose a sampler, or an intelligent mapping of the space","description":"The first version has no sampler: a study file lists explicit variants. Evidence to bring: LLM optimisers gain from warm-starting and proposal, not prediction; a budget-matched study in June 2026 found the advantage was mostly a good default; LLM optimisers stagnate rather than converge. Whatever is adopted needs the hand-tuned recipe and a seeded random search as controls.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:46Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:41:46Z","dependencies":[{"issue_id":"so-dzy","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:04Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-u85","title":"Convert instructions for Google agents","description":"On the day, with the user: AGENTS.md becomes canonical in the tool repo, procedures move out of Claude Code skills into repo documents, and beads is installed on the VM. Deferred deliberately - the user will do it with the Google agent.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-09-18T12:41:46Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:41:46Z","dependencies":[{"issue_id":"so-u85","depends_on_id":"so-4tc","type":"parent-child","created_at":"2026-09-18T13:42:04Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-atn","title":"Does the all-neumann core boundary leave NVd+ near-singular on test5?","description":"The one lead the residual data itself points at, and nobody has looked at it.\n\nCore NVd+ is 99.3 percent of the scaled SNES residual on test5steady (test4steady: 84.6 percent). test5 sets bndry_core = neumann on Nd+, Vd+, NVd+ and Pd+, where test4 uses decaylength on the SOL and PF sides and free_o3 on the core for the neutrals. Neumann on every side of the core block for a momentum variable is worth checking for a near-null-space: an undetermined core flow would show up as exactly this, and would also explain the var_scaling_factors floor at rtol being engaged.\n\nCaveat that keeps this at P3: F24 established that which equation gates the residual does NOT predict whether the solve fails - test4steady MUMPS-2 is NVd+ 85 percent with zero failures. So a residual share alone is not evidence. What would be evidence is the conditioning of the momentum block, or a run with a pinned core boundary.","acceptance_criteria":"Either a demonstrated near-null-space in the core momentum block, or a run with a non-neumann core boundary showing no change, closing the lead.","status":"closed","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-08-01T08:13:02Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:29Z","closed_at":"2026-09-18T12:42:29Z","close_reason":"Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build; these leads belong to the archived campaign and their evidence is in archive/2026-08-tuning/.","dependencies":[{"issue_id":"so-atn","depends_on_id":"so-pp3","type":"parent-child","created_at":"2026-08-01T09:13:11Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-oze","title":"Index schema has no outcome for a build that failed","description":"run_ladder.py must record an anchor that does not compile: a build failure answers the window question, and an anchor with no row is indistinguishable from one never attempted. The outcome enum in perftest-results/schema.md is completed, diverged_at_start, snes_failure, linear_failure, crawl_aborted, completed_wrong - none of which fit, so the driver currently writes state=cancelled and puts the reason in note, which no column can be filtered on.\n\nProposal for the user: add build_failed to the outcome enum. Not done unilaterally because the enum is the store's schema, shared across projects.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T17:34:03Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T17:34:03Z","dependencies":[{"issue_id":"so-oze","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T18:34:07Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-wyo","title":"perf-bisect: campaign script reading the store","description":"Deliverable is a campaign of its own, not a study inside analysis-solver-opt.py: different question, different axis (commit date rather than solver setting). It reads the results store so reports survive R8 deleting the dumps, same as the solver-opt campaign. Follows the existing convention: each study an @camp.study function with the conclusion in the docstring, pages naming what they plot and never what they found. Open question: the primary x-axis - commit date reads naturally but spaces the anchors unevenly and hides that the ladder is a sample, while commit order does the opposite.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:45:04Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T10:45:04Z","dependencies":[{"issue_id":"so-wyo","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:19Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-wyo","depends_on_id":"so-mmw","type":"blocks","created_at":"2026-07-31T11:45:24Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-wyo","depends_on_id":"so-psq","type":"blocks","created_at":"2026-07-31T11:51:24Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-34c","title":"perf-bisect: bisection procedure for refining into a step","description":"The ladder is deliberately coarse; its output is intervals, not causes. When wall time steps between two anchors, bisect into that interval with 2-3 further builds to name the commit responsible. Because runs are cheap (tens of minutes) and builds are not, this is far cheaper than a fine comb upfront and yields an attribution rather than a sampled curve. Needs writing down as a procedure, including when a step is big enough to be worth chasing and when to stop.\n\nScope ruling 2026-07-31: bisecting BOUT++ itself is OUT. If a step lands on a BOUT++ submodule bump, attributing it to that bump is the answer wanted - no attempt to separate the two codebases. Keeps the procedure to one repo.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:45:03Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T10:51:15Z","dependencies":[{"issue_id":"so-34c","depends_on_id":"so-372","type":"blocks","created_at":"2026-07-31T11:45:23Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-34c","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:18Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-5vb","title":"perf-bisect: report physics drift beside timing, and gate comparability on it","description":"A from-scratch test removes the seed-state confound but not drift: different commits follow different trajectories, so after the same simulated duration they can sit in different states, and a timing difference can reflect a different problem rather than a faster solver. The index already carries ne_sep, te_sep, ne_target_max, te_target_max and max_dev. Report them alongside wall time and declare timings incomparable past a drift threshold, so the hazard is a visible column rather than a silent confound. Open question: what threshold, and whether it should reuse whatever the physics-results-reasonable issue settles on rather than inventing a second rule.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-31T10:45:02Z","created_by":"Mike Kryjak","updated_at":"2026-07-31T10:45:02Z","dependencies":[{"issue_id":"so-5vb","depends_on_id":"so-cnv","type":"parent-child","created_at":"2026-07-31T11:45:17Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-5vb","depends_on_id":"so-gkj","type":"blocks","created_at":"2026-07-31T11:45:25Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-72i","title":"One colour convention across campaigns","description":"case_comparison already has ctx.cases.colors(), used by the shared monitor page. The solver-opt pages colour by viridis index instead (_styles), so the two campaigns colour the same case differently in reports read side by side.\n\n_styles also encodes something worth keeping and promoting: a control run - a repeat - takes the colour of what it repeats plus a dashed line, rather than the next slot on the scale.","notes":"2026-09-20 audit: premise weakened. This compares the solver-opt campaign's colours against case_comparison's, but that campaign is archived and the new project has no campaign script yet. There are no two live campaigns to reconcile until the query and report work lands. Defer rather than keep in the ready queue.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T16:47:00Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:16Z","dependencies":[{"issue_id":"so-72i","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-30T17:47:01Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-5ot","title":"Neutral pressure spikes to 100% of the residual norm (F18)","description":"The residual-weights page shows Pe holding the solver up almost everywhere, punctuated by spikes where Pd — neutral deuterium pressure — takes essentially the entire residual norm. lag_jacobian=3 shows markedly more of these spikes than the baseline.\n\nSame signature as the earlier test4 MC-vs-VanAlbada study, whose conclusion was that the slow initial phase is dominated by neutral pressure. Two independent tests pointing at the same equation is worth following.\n\nOpen question: are the spikes what the Jacobian reuse is paying for — a staler Jacobian coping worse with the neutral pressure transients — or are they present regardless and merely more visible? Cheap check: the spike count against lag_jacobian on the runs already recorded.","notes":"2026-09-20 audit: leftover from the 2026-08 tuning campaign archived on 2026-09-18. Eleven sibling issues were closed that day with the reason 'Archived with the 2026-08 tuning work. The new project restarts optimisation from a known recipe on a fixed build'. This one was left open by omission, not by decision. It should either be closed the same way or re-justified against the new campaign. The user decides. Note that so-yjs.2 is NOT in this group: it was updated on 2026-09-19 and its conclusion graduated into SNES-MUMPS-3, so it is live.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-30T13:27:11Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-xnq","title":"Record the conduction method as a dump constant (F14)","description":"The slope limiter is written into every dump as HERMES_SLOPE_LIMITER, but the conduction method (CONDMETHOD compile option) is not written anywhere. F1 has it moving test5scratch by up to 32%, so it is a first-order confounder that currently has to be declared by hand and can be silently wrong. Small C++ change in Hermes-3 mirroring how the slope limiter is recorded. Until it exists the extractor must take it as a declared field.","notes":"2026-09-20 audit: OBSOLETE as written. The CONDMETHOD compile-time option no longer exists — conduction method became a user-facing runtime option upstream on 2026-08-13 (hermes-3 60f5b421 and b8e82ea4, in src/braginskii_conduction.cxx and src/neutral_mixed.cxx), and CMakeLists.txt no longer mentions it. The C++ change this issue asks for is moot. What survives is a smaller extractor task: cli/extract_test.py still demands --conduction by hand, and schema.md still calls the column declared-not-measured. Both should read the option from the settings file instead. Retitle to that, or close and file the extractor task fresh.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T17:45:48Z","created_by":"Mike Kryjak","updated_at":"2026-09-20T15:35:12Z","dependencies":[{"issue_id":"so-xnq","depends_on_id":"so-s0b","type":"parent-child","created_at":"2026-07-29T18:45:59Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-r53.5","title":"Promote SNES iteration counts and convergence reason into dump variables","description":"SNES fetches nl_its, lin_its and SNESConvergedReason (snes.cxx:794-800) but keeps them as locals, printing them to BOUT.log only under diagnose=true. BOUT.log is overwritten on restart, so the SNES record is the most fragile part of the capture. Promoting them to dump variables via the same add_int_diagnostic mechanism CVODE already uses (cvode.cxx:179-196) would make them durable. Explicitly NOT blocking: The user wants the console-and-log route proven first, then a judgement on whether the C++ change earns its cost. Compiling is the user's job — prepare the change and hand over.","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T13:47:51Z","created_by":"Mike Kryjak","updated_at":"2026-07-29T14:46:09Z","dependencies":[{"issue_id":"so-r53.5","depends_on_id":"so-r53","type":"parent-child","created_at":"2026-07-29T14:47:51Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-eqk","title":"Choose the two short tests, and the sequence of increasing cost (R16)","description":"R16 sets a sequence of tests of increasing cost: a short test run constantly, a second short test that is hard in a different way, then the scratch test, then that run continued to convergence. Run further along the sequence only as performance improves.\n\nScope narrows the choice: SNES is what we optimise (R26) and test 5 has never run on SNES, so test 5 is reference-only until the CVODE puzzle (so-yjs.1) is solved. That leaves tests 2, 3 and 4.\n\nProposed: test4stiff as the constant short test (4m15s, full resolution, restarted at 2ms where it is genuinely stiff, and the short form of the test R25 requires us to make fast), and test3 as the second (26s, DIII-D rather than ST40, hard because everything is changing at once rather than because it is stiff). test2stiff held in reserve as a system-size contrast for phase 3.\n\nCaution on test 2: the hermes-perftest readme records its convergence reason as nearly always 4, which in PETSc means convergence on step size rather than on the residual reaching tolerance. If it stops because Newton steps became tiny rather than because the residual fell, its timings partly measure how fast a recipe stagnates, and a recipe that stagnates sooner would look faster. Verify once convergence reasons are captured, before test 2 is used for any optimisation.\n\nNote: the readme's description of test5stiff is wrong (confirmed by the user) — it repeats the test5steady text. Do not trust the readme on restart points.","acceptance_criteria":"Named tests for each rung, and the promotion rule for moving a candidate recipe up the ladder.","notes":"Deferred 2026-07-29. This is not a one-time decision — which tests to use depends on what is being optimised at the time, so it gets made per optimisation campaign rather than fixed now. The analysis above (test4stiff and test3 proposed, test 2 suspect because its convergence reason suggests stagnation rather than convergence, test 5 out of scope until SNES works there) is kept as input for whenever the question is live. None of the imminent runs need it: the instrumentation measurements and the noise-floor work need only a cheap short test, and the CVODE puzzle needs test 5 specifically.","status":"closed","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-29T10:26:42Z","created_by":"Mike Kryjak","updated_at":"2026-09-18T12:42:34Z","closed_at":"2026-09-18T12:42:34Z","close_reason":"Answered by the 2026-09-18 design: wall clock ranks with counts as diagnostics, correctness is a two-level check, the test ladder replaces the fixed sequence, and the cutoff rule replaces the crawl criterion. See design.md and candidates C10-C14.","dependencies":[{"issue_id":"so-eqk","depends_on_id":"so-ll4","type":"parent-child","created_at":"2026-07-29T13:35:19Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"so-b09","title":"Version bump + rebaseline procedure (R18): define it and make it self-recording","description":"R18 now says version bumps are deliberate — only when a needed feature lands, or when a feature's performance is itself under test — and each bump opens an epoch with its own baseline re-run. What this issue must produce: (1) the written bump procedure, repeatable step by step; (2) what gets recorded per bump — date, old and new commit, reason, baseline numbers either side; (3) where that record lives and how a result row is tied to its epoch; (4) resolve whether the expensive scratch variants (test2/4/5scratch, 45ms from scratch, 10s of hours) are inside the per-epoch baseline set or excluded — including them makes every bump cost a day or more of machine time, excluding them means version comparisons only cover the cheap, less-stiff regime.","acceptance_criteria":"A procedure someone can follow without improvising, and an epoch record that makes 'which version was this measured on' answerable from the ledger alone.","notes":"Ruled by the user 2026-09-19: a version bump must not invalidate a campaign; a quick rebaseline absorbs it, and a new version needs new parent runs from scratch (old seeds are not reused).","status":"open","priority":3,"issue_type":"task","owner":"mikekryjak@gmail.com","created_at":"2026-07-27T08:54:52Z","created_by":"Mike Kryjak","updated_at":"2026-09-19T16:26:10Z","dependencies":[{"issue_id":"so-b09","depends_on_id":"so-csm","type":"parent-child","created_at":"2026-07-29T13:35:23Z","created_by":"Mike Kryjak","metadata":"{}"},{"issue_id":"so-b09","depends_on_id":"so-nh3","type":"blocks","created_at":"2026-07-29T11:55:06Z","created_by":"Mike Kryjak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
